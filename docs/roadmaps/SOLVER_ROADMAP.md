# Solver Roadmap

## Goal

Maximize feasible city population under fixed wall-clock and CPU budgets, while preserving exact validation for every reported final layout.

The current strategy is intentionally conservative:

- Keep `auto` as the default quality path.
- Use Greedy for fast incumbents and diagnostics.
- Use LNS as the main improvement engine.
- Use CP-SAT for exact repair, polish, proof, bounds, labels, and semantic checks.
- Promote learned guidance, portfolio, GPU, distributed solving, or external solvers only after protected equal-budget evidence says they help.

Primary metrics are time to first feasible, population at 1s/5s/30s/120s, time to best, improvement per CPU-second, CP-SAT status/gap, repeatability across fixed seeds and planner workflows, and exact validation.

## Runtime Posture

The default solver path is incumbent-first:

1. `greedy` builds a fast feasible incumbent.
2. `LNS` improves the incumbent through bounded repair.
3. `CP-SAT` performs exact repair, bounded polish, or proof.
4. `auto` orchestrates the budget and keeps the best incumbent.

| Mode             | Role                           | Default Use                                                     |
| ---------------- | ------------------------------ | --------------------------------------------------------------- |
| `auto`           | Main quality path              | Recommended solver mode                                         |
| `greedy`         | Fast incumbent and diagnostics | Seed generation, baseline, counterfactual traces                |
| `LNS`            | Main improvement engine        | Adaptive repair around incumbent layouts                        |
| `CP-SAT`         | Exact backend                  | Small proofs, local repairs, bounded polishing, semantic checks |
| CP-SAT portfolio | Explicit research mode         | Only after CPU-normalized wins over single CP-SAT               |
| Learned guidance | Future feature-flag path       | Only after offline and online holdout gates pass                |

## Current Baseline

Completed details live in [SOLVER_ROADMAP_DELIVERED.md](SOLVER_ROADMAP_DELIVERED.md). The short version:

- `greedy`, `LNS`, `CP-SAT`, and `auto` are available through backend, planner, and CLI flows.
- Planner workflows now cover solve, inspect, edit, validate, reuse, explainability maps, saved layouts, and expansion comparison.
- CP-SAT road semantics are aligned with the formal per-component anchor rule, and the 2026-04-30 road-semantics scorecard reached `OPTIMAL` on the six-case single-worker suite.
- LNS has adaptive semantic operators, replay labels, budget controls, and telemetry.
- Auto uses trace-tuned LNS budget defaults while preserving the measured `0.2` CP-SAT reserve default and explicit caller overrides.
- Cross-mode scorecards, product-corpus artifacts, telemetry manifests, workflow replay artifacts, and experiment-registry draft paths exist for promotion evidence.
- The promotion-grade product corpus keeps Auto as the right default posture: 116/120 budget-policy signals keep Auto, while the remaining misses are short-budget evidence targets rather than default-policy regressions.
- Low-risk learned-ranking labels and a CPU-first Greedy offline ranker exist for diagnostics only. No learned runtime scorer has been promoted.
- CP-SAT portfolio, exact small-window DP repair, and service-master decomposition remain guarded or opt-in; they are not default Auto behavior.

## Active Priorities

The current active priority is an evidence-only tranche: strengthen LNS replay labels and deterministic opportunity features so the next learned or budget-policy gate can be evaluated fairly.

This priority does not change solver defaults, does not promote learned guidance, and does not widen CP-SAT portfolio use. It exists to collect the missing promotion evidence around the only current short-budget gaps.

### 1. Strict LNS replay labels and feature payloads

Impact: highest near-term enabling value

Why:

- LNS is the main improvement engine after Greedy, and the strongest current seam for guided search is choosing which repair window to try next.
- Existing replay labels are schema-valid but not promotion-ready; holdout signal is still too neutral for learned window ranking.
- CP-SAT is already the exact repair, proof, gap, and label backend, so it should be used to generate trustworthy counterfactual replay evidence before any scorer is introduced.

Delivered so far:

- LNS replay labels now carry feature schema `2`, CP-SAT model fingerprints, model encoding/candidate-key metadata, model-size telemetry when available, wall-clock timing, observed CP-SAT user time, and configured worker CPU budget.
- Replay feature payloads now include connectivity-shadow, empty-graph fragmentation, and service/residential candidate-loss summaries alongside the existing window occupancy/headroom features.
- Learned-ranking label telemetry and registry drafts now preserve the LNS feature schema, CP-SAT worker count, CP-SAT model fingerprints, and an input fingerprint for replay-label evidence.
- LNS replay labels now support opt-in roll-forward final-score payloads via `--roll-forward-iterations` and `--roll-forward-repair-time`. When enabled, valid replay repairs are warm-started into a bounded LNS continuation, then each label records final population deltas from the incumbent, from the repaired seed, and versus the deterministic baseline-selected replay label. Learned-ranking label snapshots, telemetry manifests, registry drafts, and text summaries carry the roll-forward budget and label counts.
- Replay state policies are now explicit. Default label generation still uses `initial-incumbent`, while strict runs can request `initial-incumbent`, `post-first-improvement`, and `post-stagnation` with bounded state-collection iterations and a separate CP-SAT repair budget.
- `lnsBenchmarkCli` and `learnedRankingLabelCli` expose the state-policy and state-collection knobs, and label telemetry/snapshots carry requested and captured state policies.
- Learned-ranking label generation now has a `strict-lns-replay` preset that records the preset in audit/registry metadata and applies the strict three-state replay policy with the standard three fixed seeds unless the caller explicitly overrides them.
- LNS replay-label readiness now reports required, captured, and missing replay state policies at both split and pressure-family level; the strict preset enables the three-state coverage gate so artifact readiness fails clearly if a requested state is absent.
- The strict artifact bundle `strict-lns-replay-labels-2026-05-04` is generated and registered under `artifacts/learned-ranking-labels/2026-05-04/strict-lns-replay-labels/` with 4,677 Greedy labels, 1,146 LNS replay labels, protected holdout, feature schema `2`, CP-SAT worker count `1`, seeds `7`, `19`, and `37`, strict three-state replay policy, and label fingerprint `fnv1a:03ab1acf`.
- The first strict bundle is evidence-only and did not pass readiness: development missed the neutral-ratio gate at `0.894`, and holdout had 0 non-neutral labels with no captured `post-first-improvement` state in any LNS pressure family.
- Replay-label corpus cases without curated seed hints now use a minimal valid anchor-road seed for label collection only; the normal LNS benchmark corpus remains unchanged, but strict replay can collect post-improvement states from intentionally weak incumbents.
- The follow-up bundle `strict-lns-replay-labels-2026-05-04-replay-seed-v2` is generated and registered under `artifacts/learned-ranking-labels/2026-05-04/strict-lns-replay-labels-replay-seed-v2/` with 4,677 Greedy labels, 1,545 LNS replay labels, protected holdout, feature schema `2`, CP-SAT worker count `1`, seeds `7`, `19`, and `37`, all three requested replay states captured in every pressure family, label fingerprint `fnv1a:850a26f9`, and LNS scale readiness passing.
- The passing strict bundle clears the label-scale gate: development has 810 usable LNS labels, 333 non-neutral labels, and neutral ratio `0.589`; holdout has 735 usable LNS labels, 363 non-neutral labels, and neutral ratio `0.506`.
- Offline LNS window-ranking baseline tooling now evaluates recorded replay decisions without running CP-SAT, reports improvement capture, hit@1, hit@k, regret, split/family/state breakdowns, telemetry, and registry drafts via `benchmark:lns-ranker`.
- The registered baseline sweep `lns-window-ranker-baselines-2026-05-04-strict-replay-seed-v2` shows useful holdout ranking signal but sets a high non-learned bar: current operator score captures `0.6307` of available holdout improvement, stable random captures `0.6429`, candidate-loss captures `0.7084`, and the best simple baseline, fragmentation, captures `0.7765` with hit@1 `0.6154`.
- CPU-first LNS window-ranker tooling now trains a linear pairwise replay model from the strict label bundle without running CP-SAT, writes model/telemetry/registry artifacts via `benchmark:lns-window-ranker`, and remains diagnostics-only with no runtime hook or solver default change.
- The registered ranker artifact `lns-window-ranker-2026-05-04-strict-replay-seed-v2` is generated under `artifacts/lns-window-ranker/2026-05-04/strict-replay-seed-v2-cpu-ranker/` with model fingerprint `fnv1a:2d6b2b9f`, dataset fingerprint `fnv1a:1c1cb973`, and label fingerprint `fnv1a:850a26f9`. It beats the best fragmentation baseline on protected holdout: model capture `0.8780` versus `0.7765`, delta `+0.1015`, hit@1 `0.6154`, hit@k `0.7692`, and mean regret `29.62`.
- LNS now has an opt-in `lns.windowRanker` runtime hook for online experiments. It accepts the offline linear model shape, scores candidate repair windows after the existing adaptive baseline selector runs, records per-outcome selection telemetry, and falls back to the deterministic selector when disabled or when `minScoreDelta` is not cleared. Solver defaults remain unchanged.
- LNS window-ranker online A/B tooling now exists behind `lnsBenchmarkCli --window-ranker-online-ablation --window-ranker-model=<model.json>`. It runs the same selected replay-pressure cases, seeds, Greedy/CP-SAT options, and LNS repair budgets for the deterministic selector and the opt-in ranker, then reports population deltas, wall-clock deltas, win/regression rates, ranker decisions, overrides, fallbacks, and model fingerprints. The companion `--window-ranker-threshold-sweep` mode compares multiple `minScoreDelta` values and reports top mean-delta and no-regression thresholds.
- Preliminary online A/B runs over the default replay-pressure corpus with seeds `7`, `19`, and `37` produced useful but threshold-sensitive signal. With `minScoreDelta=0`, the ranker improved 12/30 comparisons, regressed 3/30, left 15/30 unchanged, raised mean population by `+42.5`, and had worst delta `-165` on `lns-gate-side-channel-pressure`. A targeted threshold sweep on that regression family found `0` and `0.05` unsafe, while `0.1`, `0.15`, and `0.2` all fell back safely.
- Online LNS window-ranker A/B artifacts and registry drafts now exist for calibrated scorecards. The registered scorecard `lns-window-ranker-online-2026-05-04-strict-replay-seed-v2-min-score-delta-0-1` is generated under `artifacts/lns-window-ranker-online/2026-05-04/strict-replay-seed-v2-min-score-delta-0.1/` with model fingerprint `fnv1a:2d6b2b9f`, seeds `7`, `19`, and `37`, `minScoreDelta=0.1`, 30 equal-budget comparisons, 60 total runs, and no solver default change. It improved mean population by `+17.667`, improved 4/30 comparisons, regressed 0/30, left 26/30 unchanged, had worst delta `0`, mean wall-clock delta `-0.014s`, and recorded 18 overrides plus 39 fallback decisions. This is calibrated replay-pressure evidence, not protected holdout promotion evidence.
- A five-case independent online holdout corpus now exists behind `lnsBenchmarkCli --window-ranker-protected-holdout`, covering corridor, gate, footprint, service, and anchor-service pressure families with fresh case names. The registered protected scorecard `lns-window-ranker-online-2026-05-04-strict-replay-seed-v2-protected-holdout-min-score-delta-0-1` is generated under `artifacts/lns-window-ranker-online/2026-05-04/strict-replay-seed-v2-protected-holdout-min-score-delta-0.1/` with model fingerprint `fnv1a:2d6b2b9f`, seeds `7`, `19`, and `37`, `minScoreDelta=0.1`, 15 equal-budget comparisons, 30 total runs, protected holdout metadata, and no solver default change. It was safe but neutral: mean population delta `0`, 0/15 improved, 0/15 regressed, 15/15 unchanged, worst delta `0`, mean wall-clock delta `+0.073s`, 24 overrides, and 6 fallback decisions. This blocks promotion until protected holdout quality lift appears.
- Online A/B summaries, telemetry manifests, and registry drafts now include override outcome diagnostics. The registered diagnostic scorecard `lns-window-ranker-online-2026-05-04-strict-replay-seed-v2-protected-holdout-diagnostics-min-score-delta-0-1` confirms the neutral holdout pattern: 24 overrides cleared the `0.1` score-delta threshold, but only 6 override repairs improved an intermediate LNS step and 18 were neutral; final population still tied baseline on all 15 comparisons. The mean override score delta was about `0.168`, so the current model is confidently selecting many windows that do not create final-score lift on protected holdout.
- LNS window-ranker training now has an opt-in baseline tie-break mode (`--baseline-tie-break` / `training.baselineTieBreak`) that uses the deterministic baseline-selected label as the sole positive when it ties the best replay improvement. This is diagnostics-only and leaves existing model/runtime defaults unchanged.
- The registered tie-break ranker `lns-window-ranker-2026-05-04-strict-replay-seed-v2-baseline-tie-break` is generated under `artifacts/lns-window-ranker/2026-05-04/strict-replay-seed-v2-baseline-tie-break-cpu-ranker/` with model fingerprint `fnv1a:3dc354f1`. It improves offline protected holdout capture to `0.9350` versus the fragmentation baseline `0.7765` and the prior learned model `0.8780`; hit@1 rises to `0.7692`, hit@k to `0.9231`, and trained pair count drops from 5,270 to 2,660.
- The registered tie-break replay-pressure scorecard `lns-window-ranker-online-2026-05-04-strict-replay-seed-v2-baseline-tie-break-min-score-delta-0-1` is generated under `artifacts/lns-window-ranker-online/2026-05-04/strict-replay-seed-v2-baseline-tie-break-min-score-delta-0.1/`. At `minScoreDelta=0.1`, it improves mean population by `+34.667`, improves 10/30 comparisons, regresses 0/30, leaves 20/30 unchanged, and records 19 overrides plus 38 fallback decisions. Override diagnostics are sharper than the prior calibrated scorecard: 13 override repairs improved and 6 were neutral.
- The registered tie-break protected scorecard `lns-window-ranker-online-2026-05-04-strict-replay-seed-v2-baseline-tie-break-protected-holdout-min-score-delta-0-1` is generated under `artifacts/lns-window-ranker-online/2026-05-04/strict-replay-seed-v2-baseline-tie-break-protected-holdout-min-score-delta-0.1/`. It remains safe but neutral on independent protected holdout: mean population delta `0`, 0/15 improved, 0/15 regressed, 15/15 unchanged, 21 overrides, 9 fallbacks, 6 improved override repairs, and 15 neutral override repairs. This reduces ambiguous protected overrides but still blocks promotion because no final-score lift appears.
- Online LNS window-ranker artifacts now carry an explicit final-outcome payload that joins ranker override/fallback activity to final population status. The registered replay-pressure final-outcome scorecard `lns-window-ranker-online-2026-05-04-strict-replay-seed-v2-baseline-tie-break-final-outcome-min-score-delta-0-1` confirms durable calibration lift: override-bearing comparisons finished 10 improved, 3 neutral, 0 regressed, with mean final population delta `+80` among override-bearing comparisons. The registered protected final-outcome scorecard `lns-window-ranker-online-2026-05-04-strict-replay-seed-v2-baseline-tie-break-final-outcome-protected-holdout-min-score-delta-0-1` confirms the holdout gap: override-bearing comparisons finished 0 improved, 12 neutral, 0 regressed, with mean final delta `0`.
- The strict roll-forward label bundle `strict-lns-replay-labels-2026-05-04-replay-seed-v2-roll-forward-1x0-1` is generated under `artifacts/learned-ranking-labels/2026-05-04/strict-lns-replay-labels-replay-seed-v2-roll-forward-1x0.1/` with 4,677 Greedy labels, 1,545 LNS replay labels, one-iteration roll-forward coverage on all LNS replay labels, label fingerprint `fnv1a:5bc48690`, and dry-run registry validation only. Final-lift labels are mixed versus the deterministic baseline-selected replay label: development has 87 improved, 659 neutral, and 64 regressed labels; holdout has 168 improved, 480 neutral, and 87 regressed labels.
- LNS window-ranker training and baseline sweeps now support the opt-in target `roll-forward-final-lift` via `--final-lift-target` or `--target=roll-forward-final-lift`. The default target remains immediate replay improvement, so existing diagnostic behavior is unchanged unless a final-lift target is requested.
- The tuned final-lift ranker `lns-window-ranker-2026-05-04-strict-replay-seed-v2-roll-forward-1x0-1-final-lift-lr0-02-cap100` is generated under `artifacts/lns-window-ranker/2026-05-04/strict-replay-seed-v2-roll-forward-1x0.1-final-lift-cpu-ranker-lr0.02-cap100/` with model fingerprint `fnv1a:51fb7b72`, dataset fingerprint `fnv1a:23524ccc`, label fingerprint `fnv1a:5bc48690`, and dry-run registry validation only. It beats the final-lift fragmentation baseline offline: holdout capture `0.8339` versus `0.6310`, delta `+0.2029`, hit@1 `0.9091`, and hit@k `1.0`; no solver default changed.
- The protected online scorecard `lns-window-ranker-online-2026-05-04-strict-replay-seed-v2-roll-forward-1x0-1-final-lift-protected-holdout-min-score-delta-0-1` is generated under `artifacts/lns-window-ranker-online/2026-05-04/strict-replay-seed-v2-roll-forward-1x0.1-final-lift-protected-holdout-min-score-delta-0.1/` with dry-run registry validation only. It is safe but still neutral: mean population delta `0`, worst delta `0`, 0/15 improved, 0/15 regressed, 15/15 unchanged, 30 overrides, 0 fallbacks, and override-bearing final outcomes at 0 improved, 15 neutral, and 0 regressed. The offline final-lift improvement therefore does not yet clear the promotion blocker.
- Online ranker selection telemetry now carries baseline and selected candidate indexes, operators, and windows. Online A/B summaries, telemetry manifests, registry drafts, and text output aggregate override/fallback operator-transition counts plus changed-window counts, so protected scorecards can distinguish score-only confidence from actual geometry/operator changes.
- Protected threshold sweep artifact support now exists for the online ranker CLI, including scorecard JSON/text, telemetry manifest, registry draft, and registry dry-run validation. The regenerated protected sweep `lns-window-ranker-online-2026-05-04-strict-replay-seed-v2-roll-forward-1x0-1-final-lift-protected-holdout-threshold-sweep` is generated under `artifacts/lns-window-ranker-online/2026-05-04/strict-replay-seed-v2-roll-forward-1x0.1-final-lift-protected-holdout-threshold-sweep/` with model fingerprint `fnv1a:51fb7b72`, thresholds `0.1,0.25,0.5,0.75,1,1.5`, 5 protected cases, 3 seeds, 90 threshold comparisons, 180 total runs, and dry-run registry validation only. The top mean and top no-regression threshold are now both `1.5`, with mean delta `+3`, worst delta `0`, 1/15 improved, 0/15 regressed, and 14/15 unchanged. Because `1.5` has 0 overrides and 30 fallbacks, this is fallback-only safety/noise evidence rather than learned-ranker lift; the override-active thresholds `0.1`, `0.25`, and `0.5` still finish neutral on protected holdout.
- Online transition diagnostics now aggregate final-outcome status and protected pressure-family counts for every override/fallback transition in scorecard JSON/text, telemetry manifests, and registry drafts. The protected threshold artifact confirms the current offline-to-online gap is not just score calibration: at `0.1` and `0.25`, all 30 ranker decisions overrode the baseline and changed windows, with every override transition final-neutral. The neutral transitions were `weak-service->residential-headroom` across corridor, gate, and service-pressure families; `weak-service->frontier-congestion` across footprint-pressure and anchor-service families; and same-operator `random-exploration->random-exploration` swaps across all five protected families. At `0.5`, overrides dropped to 15/30 and fallbacks rose to 15/30, but both override and fallback transition finals remained neutral. The lone regenerated `+45` best case at `1.5` appears only on all-fallback baseline-following transitions for `lns-holdout-service-ridge-pressure` seed `19`, so it does not clear the learned override promotion blocker.
- LNS replay labels now carry explicit seed-hint diagnostics (`none`, `curated`, or `weak-replay`) plus source names, and LNS ranker/baseline tooling can aggregate seed-hint breakdowns or run with `--exclude-weak-replay-seed-labels` / `--no-weak-replay-seed-labels`. The backward-compatible ranker path can infer weak replay seeds for the existing strict label artifact from the default replay-label corpus, so old artifacts remain diagnosable.
- The weak-seed exclusion diagnostic `lns-window-ranker-2026-05-04-strict-replay-seed-v2-roll-forward-1x0-1-final-lift-no-weak-seeds-lr0-02-cap100` is generated under `artifacts/lns-window-ranker/2026-05-04/strict-replay-seed-v2-roll-forward-1x0.1-final-lift-no-weak-seeds-cpu-ranker-lr0.02-cap100/` with model fingerprint `fnv1a:2e36b926`, label fingerprint `fnv1a:5bc48690`, target `roll-forward-final-lift`, and dry-run registry validation only. It fails the offline gate by design: excluding weak replay-seed labels leaves 162 usable labels, 9 development decisions, 0 holdout decisions, 0 opportunities, and 0 trained pairs. This confirms the current offline final-lift signal is dominated by intentionally weak replay seeds; curated/real-seed replay states do not yet provide protected holdout final-lift opportunity.
- LNS window-ranker runtime telemetry now records the normalized baseline and selected feature payloads plus per-feature deltas for every online selection. Online A/B and threshold artifacts aggregate overall and transition-level mean feature deltas for override and fallback selections in JSON/text, telemetry manifests, and registry drafts, so future protected scorecards can explain which model features made each neutral operator transition look attractive without changing solver defaults.
- LNS replay-label collection now has a natural-seed corpus path for non-weak diagnostics. `lnsBenchmarkCli --window-replay-labels --natural-replay-seeds` and `learnedRankingLabelCli --natural-lns-replay-seeds` use the same replay-label case names without injecting synthetic weak replay seeds into unseeded benchmark cases, while preserving explicitly curated seed hints. Default strict replay-label behavior remains unchanged.
- The strict natural-seed roll-forward bundle `strict-lns-replay-labels-2026-05-04-natural-seeds-roll-forward-1x0-1` is generated under `artifacts/learned-ranking-labels/2026-05-04/strict-lns-replay-labels-natural-seeds-roll-forward-1x0.1/` with 4,677 Greedy labels, 1,146 LNS replay labels, label fingerprint `fnv1a:641c6c72`, and dry-run registry validation only. It is diagnostics-only and does not pass LNS scale readiness: development has 648 usable labels with 69 immediate non-neutral labels but all 648 roll-forward labels final-neutral; holdout has 498 usable labels, 0 immediate non-neutral labels, and all 498 roll-forward labels final-neutral. This confirms that simply removing synthetic weak replay seeds does not rebuild protected final-lift opportunity.
- Learned-ranking label summaries, telemetry manifests, and registry drafts now expose roll-forward final-status counts plus final-lift opportunity label/case counts at suite and split level, so future strict or natural-seed bundles report whether they contain promotion-relevant roll-forward opportunity without separate artifact scripts.
- LNS replay-label collection now has an opt-in curated non-weak seed path. `lnsBenchmarkCli --window-replay-labels --curated-replay-seeds` and `learnedRankingLabelCli --curated-lns-replay-seeds` use curated partial/first-improvement replay seeds across every strict LNS replay pressure family while leaving default weak-seed and natural-seed behavior unchanged.
- The strict curated-seed roll-forward bundle `strict-lns-replay-labels-2026-05-04-curated-seeds-roll-forward-1x0-1` is generated under `artifacts/learned-ranking-labels/2026-05-04/strict-lns-replay-labels-curated-seeds-roll-forward-1x0.1/` with 4,677 Greedy labels, 1,200 LNS replay labels, label fingerprint `fnv1a:856b8df0`, and dry-run registry validation only. It rebuilds non-weak protected final-lift opportunity but is still diagnostics-only: suite roll-forward status is 90 improved, 1,107 neutral, 3 regressed, and 0 unknown, with 90 opportunity labels across 15 replay cases. Development has 648 usable labels, 93 immediate non-neutral labels, and roll-forward status 36 improved / 609 neutral / 3 regressed; holdout has 552 usable labels, 54 immediate non-neutral labels, and roll-forward status 54 improved / 498 neutral / 0 regressed. LNS scale readiness remains false because neutral ratios are still above gate and post-first-improvement coverage is concentrated in the curated gate/service families.
- The final full-family curated-seed roll-forward bundle `strict-lns-replay-labels-2026-05-04-curated-seeds-v3-roll-forward-1x0-1` is generated under `artifacts/learned-ranking-labels/2026-05-04/strict-lns-replay-labels-curated-seeds-v3-roll-forward-1x0.1/` with 4,677 Greedy labels, 1,545 LNS replay labels, label fingerprint `fnv1a:58ce487f`, protected holdout, and dry-run registry validation only. It passes LNS label-scale readiness without synthetic weak replay seeds: development has 810 usable labels, 207 immediate non-neutral labels, neutral ratio `0.744`, and all required replay states in all five pressure families; holdout has 735 usable labels, 180 immediate non-neutral labels, neutral ratio `0.755`, and all required replay states in all five pressure families. Roll-forward final status is 264 improved, 1,276 neutral, 5 regressed, and 0 unknown, with 264 final-lift opportunity labels across 54 replay cases.
- The full-family curated non-weak final-lift baseline sweep `lns-window-ranker-baselines-2026-05-05-curated-seeds-v3-roll-forward-1x0-1-final-lift-no-weak` is generated under `artifacts/lns-window-ranker-baselines/2026-05-05/strict-lns-replay-labels-curated-seeds-v3-roll-forward-1x0.1-final-lift-no-weak-baselines/` with dry-run registry validation only. It uses `--final-lift-target --no-weak-replay-seed-labels`, keeps all 1,545 labels usable, reports 54 final-lift opportunities, and sets the offline holdout bar at fragmentation capture `0.5726` with hit@1 `0.8182`.
- The tuned full-family curated non-weak final-lift ranker `lns-window-ranker-2026-05-05-curated-seeds-v3-roll-forward-1x0-1-final-lift-no-weak-lr0-02-cap100` is generated under `artifacts/lns-window-ranker/2026-05-05/strict-lns-replay-labels-curated-seeds-v3-roll-forward-1x0.1-final-lift-no-weak-cpu-ranker-lr0.02-cap100/` with model fingerprint `fnv1a:fa22b085`, dataset fingerprint `fnv1a:b3c3fe8b`, label fingerprint `fnv1a:58ce487f`, and dry-run registry validation only. It clears the offline final-lift gate decisively: development and holdout capture are both `1.0000` with hit@1 `1.0000`, best-baseline holdout capture is `0.5726`, and holdout delta is `+0.4274`; no solver default changed.
- The protected online scorecard `lns-window-ranker-online-2026-05-05-curated-seeds-v3-roll-forward-1x0-1-final-lift-no-weak-protected-holdout-min-score-delta-0-1` is generated under `artifacts/lns-window-ranker-online/2026-05-05/strict-lns-replay-labels-curated-seeds-v3-roll-forward-1x0.1-final-lift-no-weak-protected-holdout-min-score-delta-0.1/` with dry-run registry validation only. It is safe but fully neutral on independent protected holdout: mean population delta `0`, worst delta `0`, 0/15 improved, 0/15 regressed, 15/15 unchanged, 30 overrides, 0 fallbacks, and override-bearing final outcomes at 0 improved, 15 neutral, and 0 regressed. The regenerated scorecard now carries 30 per-decision selection trace rows with baseline/selected operators, windows, scores, full feature payloads, and final-layout deltas; 8/15 protected comparisons ended with a different layout despite unchanged final population, with mean placement delta `5.6`.
- The protected threshold sweep `lns-window-ranker-online-2026-05-05-curated-seeds-v3-roll-forward-1x0-1-final-lift-no-weak-protected-holdout-threshold-sweep` is generated under `artifacts/lns-window-ranker-online/2026-05-05/strict-lns-replay-labels-curated-seeds-v3-roll-forward-1x0.1-final-lift-no-weak-protected-holdout-threshold-sweep/` with model fingerprint `fnv1a:fa22b085`, thresholds `0.1,0.25,0.5,0.75,1,1.5`, 5 protected cases, 3 seeds, and dry-run registry validation only. Every threshold is safe but neutral: top mean and top no-regression threshold are both `0.1`, mean delta `0`, worst delta `0`, 0/15 improved, and 0/15 regressed. Thresholds `0.1`, `0.25`, and `0.5` override all 30 ranker decisions and every override transition is final-neutral; `0.75` overrides 6/30 with final-neutral outcomes; `1` and `1.5` are all-fallback and final-neutral. This keeps learned LNS ranking diagnostics-only despite the offline-perfect v3 final-lift model.
- Offline-to-online gap diagnostics now exist behind `lnsWindowRankerCli --gap-diagnostics`, joining trained-model replay decisions from a label artifact to protected online scorecard override transitions without rerunning CP-SAT or LNS. The registered diagnostic draft `lns-window-ranker-gap-2026-05-05-curated-seeds-v3-final-lift-no-weak-protected` is generated under `artifacts/lns-window-ranker-gap-diagnostics/2026-05-05/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-protected-gap/` with dry-run registry validation only and regenerated online scorecard fingerprint `fnv1a:17e1fd8e`. It confirms the blocker has two concrete shapes: 30/30 online overrides are final-neutral; 3 protected transition/family pairs are offline holdout-positive but online final-neutral (`corridor/random-exploration->random-exploration`, `footprint-pressure/weak-service->sliding`, and `gate/weak-service->sliding`); and 6 protected transition/family pairs are online-active with no matching offline holdout transition. The diagnostic now records `selection-trace=30` plus 9 trace comparisons with sample selected windows, top offline-vs-online feature-delta gaps, final-layout deltas, layout signatures, ranker-trace trajectory-after-selection payloads, explicit recommended experiments, and diagnostic-only promotion sensitivity passes. Among the 27 online traces covered by trace comparisons, 14 changed the final layout. Signature counts are 4 `changed-layout-final-neutral`, 4 `zero-layout-final-neutral`, and 1 `mixed-layout-final-neutral`. Suppressing the 4 zero-layout/final-neutral comparisons still leaves 5 blocked comparisons: 4 with unmatched protected replay evidence and all 5 with changed-layout/no-lift trajectory-depth signatures. Gating the unmatched online-only comparisons until matching protected replay evidence exists suppresses those 4 and leaves 1 evidence-backed blocker: changed-layout/no-lift trajectory depth. The remaining evidence-backed blocker is `corridor/random-exploration->random-exploration`: all 3 traces changed final layouts with mean placement delta `5`, but the trajectory payload reports 0 post-selection ranker-traced repairs, 0 post-selection improvements, and 0 final population delta from the selected repair. The generated recommendation list now marks that as priority 1 for longer/chunked roll-forward replay, then marks 4 priority-2 targeted protected replay-label gaps for `service-pressure/random-exploration->random-exploration`, `service-pressure/weak-service->sliding`, `corridor/weak-service->sliding`, and `anchor-service/random-exploration->random-exploration`. Promotion remains blocked.
- `lnsBenchmarkCli --window-replay-labels` now supports `--window-replay-protected-holdout` and `--window-replay-artifact-dir`, so protected replay-label experiments can be scoped to the independent online holdout corpus and saved as JSON/text artifacts without promoting a model. The priority-1 corridor experiment `protected-corridor-random-exploration-longer-roll-forward-4x0.1` is generated under `artifacts/lns-window-replay-labels/2026-05-06/protected-corridor-random-exploration-longer-roll-forward-4x0.1/` for `lns-holdout-corridor-weave-pressure`, seeds `7`, `19`, and `37`, all strict replay states, 162 labels, and 4x0.1s roll-forward. It found no final-lift roll-forward opportunity: every transition had best final delta `0` versus the deterministic replay baseline. However, the replay-state baseline operator was `weak-service` in all 9 captured states, so that artifact did not directly reproduce the online `random-exploration->random-exploration` decision state.
- Online LNS window-ranker scorecards now capture diagnostics-only decision-state payloads for each ranker selection, and `lnsBenchmarkCli --window-replay-labels --window-replay-online-scorecard=<scorecard.json>` can warm-start protected replay labels from those exact online incumbents. The corridor decision-state capture scorecard is generated under `artifacts/lns-window-ranker-online/2026-05-06/protected-corridor-decision-state-capture-min-score-delta-0.1/` with 1 protected case, seeds `7`, `19`, and `37`, 3 comparisons, and decision-state payloads in the 6 selection traces. The exact online-state replay artifact `protected-corridor-online-decision-state-roll-forward-4x0.1` is generated under `artifacts/lns-window-replay-labels/2026-05-06/protected-corridor-online-decision-state-roll-forward-4x0.1/` with 6 online decision states, 12 labels, and 4x0.1s roll-forward. It directly reproduces the priority-1 `corridor/random-exploration->random-exploration` blocker: across all 3 seeds, the online-selected and online-baseline random-exploration windows both repair to `+65` immediately and tie after roll-forward, with final delta `0` and final status `neutral`. The corridor blocker is therefore evidence for harmless final-neutral window reshuffling, not missed protected final-lift.
- Gap diagnostics now accept exact supplemental replay snapshots through `lnsWindowRankerCli --gap-diagnostics --supplemental-replay-labels=<lns-window-replay-labels.json>`, folding those replay decisions into holdout transition evidence while preserving diagnostic-only runtime and solver defaults. The targeted priority-2 decision-state scorecard is generated under `artifacts/lns-window-ranker-online/2026-05-06/protected-priority2-decision-state-capture-min-score-delta-0.1/` with model fingerprint `fnv1a:fa22b085`, online scorecard fingerprint `fnv1a:54a5bc08`, 3 protected cases (`corridor`, `service-pressure`, and `anchor-service`), seeds `7`, `19`, and `37`, 9 comparisons, 18 selection traces, and 18 final-neutral overrides.
- The priority-2 exact online-state replay artifact `protected-priority2-online-decision-state-roll-forward-4x0.1` is generated under `artifacts/lns-window-replay-labels/2026-05-06/protected-priority2-online-decision-state-roll-forward-4x0.1/` with 18 online decision states, 36 labels, 36 roll-forward labels, and supplemental replay fingerprint `fnv1a:0f5494fc`. It closes the four prior priority-2 online-active/no-offline-match recommendations: `service-pressure/random-exploration->random-exploration`, `service-pressure/weak-service->sliding`, `corridor/weak-service->sliding`, and `anchor-service/random-exploration->random-exploration`. Every covered online-selected and online-baseline transition is final-neutral after 4x0.1s roll-forward, so the priority-2 gaps do not show protected final-lift opportunity.
- The supplemental targeted gap diagnostic `lns-window-ranker-gap-2026-05-06-curated-seeds-v3-final-lift-no-weak-protected-priority2-supplemental` is generated under `artifacts/lns-window-ranker-gap-diagnostics/2026-05-06/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-protected-priority2-supplemental-gap/`. It consumes the exact replay artifact as 18 supplemental holdout decisions, drops `online-active-no-offline-match` from 4 targeted recommendations to `0`, and removes all targeted protected replay-label recommendations. Gap diagnostics now distinguish exact online-decision supplemental replay from older generic offline replay when they disagree. In the regenerated artifact, the remaining `corridor/random-exploration->random-exploration` conflict is marked `exact-replay-neutralized=true`: older generic offline holdout labels still summarize 6 selected-positive decisions, but the exact protected online-state replay has 3 matching supplemental decisions with 0 exact selected-positive final-lift. The targeted diagnostic now reports `offline-positive-online-neutral=0`, `exact-replay-neutralized=1`, no trace comparisons, no recommended experiments, and `promotion-blocked=false`.
- The full protected exact online-decision scorecard/replay/gap diagnostic is now generated across all five independent holdout pressure families. The scorecard `protected-full-decision-state-capture-min-score-delta-0.1` is under `artifacts/lns-window-ranker-online/2026-05-06/protected-full-decision-state-capture-min-score-delta-0.1/` with online scorecard fingerprint `fnv1a:85259245`, 5 cases, 3 seeds, 15 comparisons, 30 final-neutral overrides, mean population delta `0`, worst delta `0`, and 0 regressions. The exact replay artifact `protected-full-online-decision-state-roll-forward-4x0.1` is under `artifacts/lns-window-replay-labels/2026-05-06/protected-full-online-decision-state-roll-forward-4x0.1/` with supplemental replay fingerprint `fnv1a:77b0ee06`, 30 online decision states, 60 labels, and 60 roll-forward labels across `corridor`, `gate`, `footprint-pressure`, `service-pressure`, and `anchor-service`.
- The full supplemental gap diagnostic `lns-window-ranker-gap-2026-05-06-curated-seeds-v3-final-lift-no-weak-protected-full-supplemental` is generated under `artifacts/lns-window-ranker-gap-diagnostics/2026-05-06/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-protected-full-supplemental-gap/`. It consumes 30 supplemental exact holdout decisions, covers 10 protected transition/family joins, and reports `offline-positive-online-neutral=0`, `online-active-no-offline-match=0`, `exact-replay-neutralized=3`, no trace comparisons, no recommended experiments, and `promotion-blocked=false` for diagnostic mismatch. The neutralized generic-offline positives are `corridor/random-exploration->random-exploration`, `footprint-pressure/weak-service->sliding`, and `gate/weak-service->sliding`; each has exact online-decision supplemental evidence with 0 selected-positive final-lift. This clears the replay-evidence/feature-payload diagnostic mismatch, but it does not promote the learned LNS ranker: the full protected online scorecard still shows 0/15 final-score lift.
- LNS window-ranker training now has an explicit diagnostics-only `--supplemental-replay-calibration` path. When exact supplemental `online-decision` replay groups have no final-lift opportunity, the trainer treats the deterministic baseline-selected replay window as a hard-neutral preference using the full margin cap. The calibrated model `lns-window-ranker-2026-05-06-curated-seeds-v3-final-lift-no-weak-full-exact-neutral-calibration-lr0-02-cap100` is generated under `artifacts/lns-window-ranker/2026-05-06/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-full-exact-neutral-calibration-lr0.02-cap100/` with model fingerprint `fnv1a:759a0f1a`, dataset fingerprint `fnv1a:4d731af9`, 30 supplemental replay decisions, 60 supplemental replay labels, and `passed=false` by construction because protected supplemental calibration cannot promote a model. Offline holdout capture drops to `0.3248` versus the fragmentation baseline `0.5726`, which is acceptable for this suppression-only diagnostic.
- The calibrated protected scorecard `protected-full-exact-neutral-calibration-min-score-delta-0.1` is generated under `artifacts/lns-window-ranker-online/2026-05-06/protected-full-exact-neutral-calibration-min-score-delta-0.1/` with online scorecard fingerprint `fnv1a:433f1833`. It suppresses all learned protected overrides: overrides drop from 30/30 to `0/30`, fallbacks rise to `30/30`, layout changes drop to `0`, mean population delta remains `0`, worst delta remains `0`, and regressions remain `0`. The calibrated gap diagnostic under `artifacts/lns-window-ranker-gap-diagnostics/2026-05-06/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-protected-full-exact-neutral-calibration-gap/` records `onlineOverrideCount=0`, no joined evidence, no trace comparisons, no recommendations, and `promotionBlocked=false`. This proves exact neutral calibration can suppress harmful-neutral protected overrides, but it also removes the offline final-lift behavior the old model had learned.
- The deeper protected exact online-state replay artifact `protected-full-online-decision-state-roll-forward-8x0.2` is generated under `artifacts/lns-window-replay-labels/2026-05-08/protected-full-online-decision-state-roll-forward-8x0.2/` with the same 5 independent holdout families, seeds `7`, `19`, and `37`, 30 online decision states, 60 labels, and 60 roll-forward labels. It doubles the prior exact replay depth from 4x0.1s to 8x0.2s. The result is still fully final-neutral: 60/60 roll-forward labels have final delta `0`, with 0 improved and 0 regressed labels in every protected family. This closes the simple "longer roll-forward" path for the current protected online decision states; future LNS data-generation work needs new protected states/cases with genuine final-lift opportunity, not more depth on these same neutral reshuffles.
- The protected wide-window replay artifact `protected-anchor-service-wide-window-opportunity-roll-forward-1x0.1` is generated under `artifacts/lns-window-replay-labels/2026-05-08/protected-anchor-service-wide-window-opportunity-roll-forward-1x0.1/` for `lns-holdout-anchor-service-shelf-pressure`, seeds `7`, `19`, and `37`, all strict replay states, `max-windows=16`, `exploration-windows=8`, and 1x0.1s roll-forward. It finds real protected final-lift opportunity in the existing independent holdout corpus: 216/216 labels are usable, 18 labels finish improved versus the deterministic baseline-selected replay label, 198 finish neutral, and 0 regress. The opportunities are stable across all three seeds on the `initial-incumbent` and `post-stagnation` states: `frontier-congestion`, `gate-choke`, and `random-exploration` all expose the same `2,1:3x3` repair window with final delta `+110`. This changes the next LNS data target from inventing new holdout cases to improving candidate coverage/training so protected opportunity windows are represented without allowing the exact-online neutral reshuffles to become overrides.
- The wide-window/exact-neutral calibration ranker `lns-window-ranker-2026-05-08-curated-seeds-v3-final-lift-no-weak-wide-anchor-opportunity-exact-neutral-calibration-lr0-02-cap100` is generated under `artifacts/lns-window-ranker/2026-05-08/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-wide-anchor-opportunity-exact-neutral-calibration-lr0.02-cap100/` with model fingerprint `fnv1a:79bd43a0`, dataset fingerprint `fnv1a:a76e1f9f`, 39 supplemental replay decisions, and 276 supplemental replay labels from the protected exact-online neutral replay plus the wide-window anchor-service opportunity replay. It remains diagnostics-only and does not pass the offline gate: development capture improves to `0.6250`, but protected holdout capture stays `0.3248` versus the fragmentation baseline `0.5726`.
- The protected online guardrail for that model at `minScoreDelta=0.1` is generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-full-wide-anchor-opportunity-exact-neutral-calibration-min-score-delta-0.1/` and is population-safe but behaviorally blocked: mean delta `0`, worst delta `0`, and 0 regressions, but 24/30 ranker decisions override and all 24 override-bearing outcomes finish final-neutral. The paired gap diagnostic under `artifacts/lns-window-ranker-gap-diagnostics/2026-05-08/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-wide-anchor-opportunity-exact-neutral-calibration-protected-gap/` reports `promotionBlocked=true`, one offline-positive/online-neutral join (`anchor-service/weak-service->random-exploration`), and four online-active/no-offline-match transition-family gaps. The highest protected neutral override score delta is `1.1968`.
- The suppression guardrail rerun for the same model at `minScoreDelta=1.5` is generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-full-wide-anchor-opportunity-exact-neutral-calibration-min-score-delta-1.5/`, with the matching gap diagnostic under `artifacts/lns-window-ranker-gap-diagnostics/2026-05-08/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-wide-anchor-opportunity-exact-neutral-calibration-protected-min-score-delta-1.5-gap/`. This threshold suppresses all learned protected overrides: 30/30 decisions fall back, mean delta `0`, worst delta `0`, 0 regressions, `onlineOverrideCount=0`, `offlinePositiveOnlineNeutralCount=0`, no recommended experiments, and `promotionBlocked=false`. It is a useful safety threshold, not promotion evidence, because it also contributes no protected online lift.
- `lnsBenchmarkCli --window-ranker-online-ablation` now exposes `--lns-iterations=<n>` / `--iterations=<n>` for diagnostics that need to vary online LNS trajectory length while keeping baseline and ranker budgets equal. The focused one-iteration protected anchor-service diagnostic `protected-anchor-service-wide-opportunity-one-iteration-min-score-delta-0.1` is generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-anchor-service-wide-opportunity-one-iteration-min-score-delta-0.1/`, with the matching gap diagnostic under `artifacts/lns-window-ranker-gap-diagnostics/2026-05-08/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-protected-anchor-service-one-iteration-gap/`. With only one LNS iteration, the ranker-selected `2,1:3x3` anchor-service window produces real equal-budget lift: mean delta `+110`, worst delta `+110`, 3/3 improved, 0 regressions, `onlineOverrideCount=3`, `onlineFinalNeutralOverrideCount=0`, and `promotionBlocked=false`. This reframes the two-iteration neutral result as a trajectory-timing issue: the ranker finds the protected opportunity one step earlier, but the deterministic baseline catches up by the second iteration.
- The full protected one-iteration diagnostic `protected-full-wide-anchor-opportunity-one-iteration-min-score-delta-0.1` is generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-full-wide-anchor-opportunity-one-iteration-min-score-delta-0.1/` and confirms the trajectory value generalizes safely across all five protected families: mean delta `+22`, worst delta `0`, 3/15 improved, and 0 regressions. However, the matching gap diagnostic under `artifacts/lns-window-ranker-gap-diagnostics/2026-05-08/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-protected-full-one-iteration-gap/` remains behaviorally blocked because 12 neutral protected overrides still fire alongside the 3 useful anchor-service overrides. The selective-threshold rerun `protected-full-wide-anchor-opportunity-one-iteration-min-score-delta-1.18` is generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-full-wide-anchor-opportunity-one-iteration-min-score-delta-1.18/`, with matching gap diagnostics under `artifacts/lns-window-ranker-gap-diagnostics/2026-05-08/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-protected-full-one-iteration-min-score-delta-1.18-gap/`. The `1.18` threshold separates the useful anchor-service score delta `1.1968` from the highest neutral override score delta `1.1536`: it keeps exactly 3 anchor-service overrides, suppresses 12 neutral overrides, preserves mean delta `+22`, worst delta `0`, 0 regressions, `onlineFinalNeutralOverrideCount=0`, and `promotionBlocked=false`.
- The protected threshold-band sweeps `protected-full-wide-anchor-opportunity-one-iteration-threshold-band` and `protected-full-wide-anchor-opportunity-default-trajectory-threshold-band` are generated under `artifacts/lns-window-ranker-online/2026-05-08/` with thresholds `1.12`, `1.15`, `1.18`, `1.2`, and `1.25`, seeds `7`, `19`, and `37`, all five protected families, and dry-run registry validation. In the one-iteration sweep, `1.12`, `1.15`, and `1.18` all preserve mean delta `+22`, worst delta `0`, 3/15 improvements, and 0 regressions; `1.12` and `1.15` still fire 3 service-pressure final-neutral overrides, while `1.18` keeps only the 3 anchor-service final-improving overrides. Thresholds `1.2` and `1.25` suppress every override and return to mean delta `0`. In the default two-iteration sweep, every threshold is population-safe but final-neutral: `1.12`, `1.15`, and `1.18` still fire protected overrides, all of them final-neutral, while `1.2` and `1.25` suppress all overrides. This confirms `1.18` is the best current selective threshold for shorter-trajectory value, not durable two-iteration final-population lift.
- Online LNS window-ranker scorecards now report time-to-best trajectory metrics in scorecard JSON/text, telemetry manifests, registry drafts, and threshold summaries. The refreshed `protected-full-wide-anchor-opportunity-one-iteration-min-score-delta-1.18` scorecard still reports mean population delta `+22`, worst delta `0`, 3/15 improvements, and 0 regressions; its ranker time-to-best iteration delta is `+0.2` with 0 earlier, 12 same, 3 later, and 0 unknown cases because the 3 anchor-service wins reach a new higher best after one repair while baseline stays at its lower seed value. The refreshed default two-iteration threshold band exposes the trajectory value directly: active thresholds `1.12`, `1.15`, and `1.18` all remain final-neutral with mean delta `0`, but report mean time-to-best iteration delta `-0.2` with 3 earlier, 12 same, 0 later, and 0 unknown cases. This makes the current result explicit: the ranker reaches the same protected best one completed LNS repair earlier on the anchor-service seeds, then the deterministic baseline catches up by the second iteration.
- The protected trajectory-horizon validation scorecards for `2`, `3`, and `4` LNS iterations at `minScoreDelta=1.18` are generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-full-wide-anchor-opportunity-{two,three,four}-iteration-min-score-delta-1.18/`. All three beyond-one-iteration runs are safe and final-neutral: mean population delta `0`, worst delta `0`, 0/15 improved, 0/15 regressed, and 15/15 unchanged. The trajectory signal is stable, not a two-iteration accident: each run keeps exactly 3 anchor-service overrides, those overrides are final-neutral once the deterministic baseline catches up, and the ranker reports mean time-to-best iteration delta `-0.2` with 3 earlier, 12 same, 0 later, and 0 unknown cases. At 3 and 4 iterations, the extra budget only adds fallback decisions after the useful first override (`42` and `57` fallbacks respectively), so current evidence validates protected time-to-best value beyond one iteration but not durable final-population lift.
- The independent protected online holdout corpus is broadened from 5 to 10 cases by adding a second fresh case in each existing pressure family: `lns-holdout-corridor-switchback-pressure`, `lns-holdout-gate-double-choke-pressure`, `lns-holdout-footprint-lattice-pressure`, `lns-holdout-service-braid-pressure`, and `lns-holdout-anchor-service-harbor-pressure`. The broadened one-iteration scorecard at `minScoreDelta=1.18` is generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-broadened-coverage-one-iteration-min-score-delta-1.18/` with 10 cases, 3 seeds, and 30 comparisons. It remains safe and positive but diluted: mean delta `+11`, worst delta `0`, 3/30 improved, and 0 regressions. The useful wins are still the original anchor-service shelf seeds; the broader coverage adds 6 final-neutral overrides in the new gate and service-pressure cases.
- The broadened two-iteration scorecard at `minScoreDelta=1.18` is generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-broadened-coverage-two-iteration-min-score-delta-1.18/` and remains safe/final-neutral with mean delta `0`, worst delta `0`, 0/30 improved, and 0 regressions. It still reports time-to-best value on the original anchor-service shelf seeds: mean time-to-best iteration delta `-0.1` with 3 earlier, 27 same, 0 later, and 0 unknown cases. The broadened one-iteration threshold band under `artifacts/lns-window-ranker-online/2026-05-08/protected-broadened-coverage-one-iteration-threshold-band/` shows scalar `minScoreDelta` is no longer enough to cleanly isolate the useful anchor-service lift: `1.18` keeps mean `+11` but permits 6 neutral `weak-service->sliding` overrides; `1.2` and `1.45` suppress the anchor lift while leaving those 6 neutral overrides; `1.48` leaves 3 service-pressure neutral overrides; and `1.5` suppresses every override. Broader protected coverage therefore keeps learned LNS diagnostics-only and points the next work toward transition/family-aware gating or improved features, not a scalar threshold.
- Ranker selectivity is now represented as an explicit diagnostics-only operator-transition allowlist: `lns.windowRanker.allowedTransitions` and `lnsBenchmarkCli --window-ranker-allowed-transitions=<baseline->selected,...>`. The broadened transition-allowlist scorecards at `minScoreDelta=1.18` are generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-broadened-coverage-transition-allowlist-one-iteration-min-score-delta-1.18/` and `artifacts/lns-window-ranker-online/2026-05-08/protected-broadened-coverage-transition-allowlist-two-iteration-min-score-delta-1.18/`, allowing only `weak-service->random-exploration`. The one-iteration run preserves the useful protected lift while suppressing the 6 neutral `weak-service->sliding` overrides: mean delta `+11`, worst delta `0`, 3/30 improved, 0 regressions, 3 overrides, 27 fallbacks, and 0 neutral override outcomes. The two-iteration run remains final-neutral but keeps the trajectory value: mean delta `0`, worst delta `0`, 0 regressions, 3 overrides, 57 fallbacks, and mean time-to-best iteration delta `-0.1` with 3 earlier, 27 same, 0 later, and 0 unknown cases. This finishes the current ranker-selectivity evidence bundle but is still not a solver default change; the allowlist is manual, transition-level, and diagnostic.
- Promotion-gate rechecks for the broadened transition allowlist are generated under `artifacts/lns-window-ranker-gap-diagnostics/2026-05-08/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-protected-broadened-transition-allowlist-one-iteration-min-score-delta-1.18-gap/` and `artifacts/lns-window-ranker-gap-diagnostics/2026-05-08/strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-protected-broadened-transition-allowlist-two-iteration-min-score-delta-1.18-gap/`. The one-iteration allowlist now clears the diagnostic promotion blocker on the broadened protected corpus: `promotionBlocked=false`, `offlinePositiveOnlineNeutralCount=0`, `onlineActiveNoOfflineMatchCount=0`, `onlineOverrideCount=3`, and `onlineFinalNeutralOverrideCount=0`. The two-iteration recheck remains blocked for final-lift promotion even though it is safe and earlier-to-best: `promotionBlocked=true`, `offlinePositiveOnlineNeutralCount=1`, `onlineFinalNeutralOverrideCount=3`, and the remaining evidence-backed blocker is `anchor-service/weak-service->random-exploration` with a mixed-layout final-neutral signature after the baseline catches up. Treat the current allowlist as a passing one-iteration final-lift diagnostic and a failing default-trajectory final-lift gate, not a solver-default promotion.
- Online LNS window-ranker scorecards now carry an explicit wall-clock time-to-best promotion axis in summary JSON/text, telemetry manifests, registry drafts, and threshold summaries: known/unknown pair counts, mean and median wall-clock ratios versus baseline, 10%-faster and 10%-slower counts/rates, equal-population gate status, and the final `timeToBestPromotionGatePassed` boolean. The protected broadened transition-allowlist rerun `protected-broadened-coverage-transition-allowlist-two-iteration-ttb-gate-min-score-delta-1.18` is generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-broadened-coverage-transition-allowlist-two-iteration-ttb-gate-min-score-delta-1.18/` with 10 protected cases, 3 seeds, 30 comparisons, and 60 total runs. It remains population-equal and safe: mean delta `0`, worst delta `0`, 0 regressions, and `equalPopulationTimeToBestGatePassed=true`. It still fails the new time-to-best promotion gate: all 30 time-to-best wall-clock pairs are known, mean ratio is `0.912`, median ratio is `0.967`, 10/30 rows are at least 10% faster, 4/30 are at least 10% slower, and `timeToBestPromotionGatePassed=false`. The product-facing time-to-best promotion axis is now measurable, but the current default-trajectory ranker does not clear it.
- Online LNS window-ranker scorecards now also split override and fallback feature deltas by final outcome (`improved`, `neutral`, `regressed`) and report improved-vs-neutral / regressed-vs-neutral feature gaps. The protected broadened one-iteration no-allowlist selectivity diagnostic `protected-broadened-coverage-one-iteration-feature-selectivity-min-score-delta-1.18` is generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-broadened-coverage-one-iteration-feature-selectivity-min-score-delta-1.18/` with 10 protected cases, 3 seeds, and 30 comparisons. It reproduces the prior one-iteration shape: mean delta `+11`, worst delta `0`, 3/30 improved, 0 regressions, 9 overrides, and 21 fallbacks. The final-outcome feature split exposes a learnable selectivity signal behind the manual transition allowlist: the 3 improved overrides are `weak-service->random-exploration` on `anchor-service`, while the 6 neutral overrides are `weak-service->sliding` on `gate` and `service-pressure`; improved-minus-neutral feature gaps include `operatorScore=-2.38`, `residentialCandidateHeadroom=-1.335`, `serviceCandidateBonus=-0.465`, `residentialCandidatesBlocked=-0.425`, and `serviceCountInside=+0.25`. This does not promote runtime guidance, but it gives the next model iteration concrete feature targets for replacing the manual transition allowlist.
- Ranker selectivity can now be expressed as diagnostics-only feature-delta gates via `lns.windowRanker.featureDeltaGates` and `lnsBenchmarkCli --window-ranker-feature-delta-gates=<feature<=max|feature>=min,...>`. The protected broadened one-iteration feature-gate scorecard `protected-broadened-coverage-one-iteration-feature-gate-operator-score-max-neg6-min-score-delta-1.18` is generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-broadened-coverage-one-iteration-feature-gate-operator-score-max-neg6-min-score-delta-1.18/` with `operatorScore<=-6`, 10 protected cases, 3 seeds, 30 comparisons, and registry append dry-run validation. It matches the manual transition allowlist without hard-coding transitions: mean delta `+11`, worst delta `0`, 3/30 improved, 0 regressions, 3 overrides, 27 fallbacks, 0 neutral override outcomes, and the only remaining override transition is `weak-service->random-exploration` on `anchor-service`. This is better selectivity plumbing and evidence, not a solver default promotion; the one-iteration feature gate still fails the time-to-best promotion axis because the improved rows necessarily reach a higher best later than baseline.
- The matching two-iteration feature-gate scorecard `protected-broadened-coverage-two-iteration-feature-gate-operator-score-max-neg6-min-score-delta-1.18` is generated under `artifacts/lns-window-ranker-online/2026-05-08/protected-broadened-coverage-two-iteration-feature-gate-operator-score-max-neg6-min-score-delta-1.18/` with registry append dry-run validation. It matches the manual transition allowlist at the default trajectory: mean delta `0`, worst delta `0`, 0/30 improved, 0 regressions, 3 overrides, 57 fallbacks, 3 earlier-to-best rows, mean time-to-best iteration delta `-0.1`, mean wall-clock time-to-best ratio `0.885`, median ratio `0.972`, 7/30 at least 10% faster, 0/30 at least 10% slower, and `timeToBestPromotionGatePassed=false`. The paired gap diagnostic `strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-protected-broadened-feature-gate-operator-score-max-neg6-two-iteration-min-score-delta-1.18-gap` remains blocked with `promotionBlocked=true`, `offlinePositiveOnlineNeutralCount=1`, `onlineActiveNoOfflineMatchCount=0`, `onlineFinalNeutralOverrideCount=3`, one mixed-layout final-neutral trace comparison, and one longer-roll-forward recommendation. Feature gating therefore replaces the manual transition allowlist mechanically, but it still leaves learned LNS diagnostics-only for the default two-iteration trajectory.
- The broadened feature-gated trajectory horizon is now extended to 3 and 4 LNS iterations under `protected-broadened-coverage-three-iteration-feature-gate-operator-score-max-neg6-min-score-delta-1.18` and `protected-broadened-coverage-four-iteration-feature-gate-operator-score-max-neg6-min-score-delta-1.18`, both with registry append dry-run validation. Both runs stay population-safe and final-neutral across the same 10 protected cases and 3 seeds: mean delta `0`, worst delta `0`, 0/30 improved, 0 regressions, and exactly 3 overrides. The trajectory value remains stable but below the product promotion bar. At 3 iterations, the ranker has 3 earlier-to-best rows, mean time-to-best iteration delta `-0.1`, mean wall-clock ratio `0.902`, median ratio `0.996`, 9/30 at least 10% faster, 0 slower, and `timeToBestPromotionGatePassed=false`; at 4 iterations, it has 3 earlier, mean ratio `0.892`, median ratio `0.947`, 8/30 faster, 2/30 slower, and still fails the time-to-best gate. Broadened feature-gated evidence therefore validates stable safe trajectory value through 4 iterations, but not durable final-population lift or wall-clock promotion readiness.
- Supplemental pairwise calibration was tested as a learned replacement for the manual feature gate. Two broadened online-decision replay artifacts are generated from the no-gate feature-selectivity scorecard: `protected-broadened-one-iteration-online-decision-feature-selectivity-roll-forward-1x0.1` captures all 30 broadened protected one-iteration decision states with 249 roll-forward labels, and `protected-broadened-active-overrides-online-decision-feature-selectivity-roll-forward-1x0.1` narrows to the 9 active override states with 81 labels. The replay signal is clean: 3 `weak-service->random-exploration` anchor-service selected states are `+110`, while 6 `weak-service->sliding` gate/service-pressure selected states are neutral. However, four calibrated model candidates do not improve promotion readiness. The all-state and active-only calibrations without feature masking learn a strong positive `selectedByBaseline` identity weight and suppress every protected override: one- and two-iteration online scorecards both have mean delta `0`, 0 improvements, 0 regressions, 0 overrides, and no time-to-best promotion. The opt-in trainer flag `--supplemental-replay-calibration-ignore-baseline-feature` prevents neutral supplemental pairs from updating the `selectedByBaseline` feature, but the active-only masked model still suppresses all overrides, and the all-state masked model regresses the two-iteration default trajectory with mean delta `-4.5`, worst `-45`, 3/30 regressions, and 6 unsafe `random-exploration` overrides. This closes the simple pairwise-calibration replacement path: manual feature gates remain the safer diagnostics-only selectivity control until the ranker has either a stronger model class or new durable final-lift states.
- The first stronger-model-class spike adds diagnostics-only pairwise interaction weights for the LNS window ranker. Runtime scoring now accepts optional `interactionWeights`, offline training can emit all base-feature pair interactions through `lnsWindowRankerCli --feature-interactions`, and validation/tests cover finite known-feature interaction keys. The interaction calibrated ranker `strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-wide-anchor-opportunity-exact-neutral-calibration-interactions-lr0.02-cap100` is generated under `artifacts/lns-window-ranker/2026-05-08/` with model fingerprint `fnv1a:80931913`. It improves offline holdout capture versus the calibrated linear model (`0.4786` versus `0.3248`) but still fails the offline gate against fragmentation (`0.5726`). The no-manual-gate broadened protected one-iteration threshold sweep `protected-broadened-coverage-one-iteration-interaction-ranker-threshold-sweep` then blocks the interaction model online: every tested threshold from `0` through `2` overrides all 30 decisions, mean delta is `+5`, worst delta is `-60`, regressions are 3/30, `topSafeMinScoreDelta=null`, and the unsafe rows are footprint-lattice `weak-service->random-exploration` overrides. Pairwise interactions therefore improve offline fit but do not replace the manual feature gate or justify solver-default exposure.
- The interaction model is also blocked when combined with the current safest feature gate. The broadened protected one-iteration scorecard `protected-broadened-coverage-one-iteration-interaction-ranker-feature-gate-operator-score-max-neg6-min-score-delta-1.18` is generated under `artifacts/lns-window-ranker-online/2026-05-08/` with model fingerprint `fnv1a:80931913`, `minScoreDelta=1.18`, and `operatorScore<=-6`. It still regresses the protected corpus: mean delta `+5`, worst delta `-60`, 3/30 improvements, 3/30 regressions, 27 overrides, and 3 fallbacks. The failure is the same footprint-lattice shape under all three seeds: `weak-service->random-exploration` clears the gate, the ranker finishes at `620`, and baseline reaches `680`. This closes pairwise interactions as a near-term selectivity path; the existing linear feature-gated ranker is strictly safer on the same broadened one-iteration evidence.
- The focused durable-follow-up replay `protected-anchor-service-feature-gate-two-iteration-online-decision-wide-replay-roll-forward-4x0.2` is generated under `artifacts/lns-window-replay-labels/2026-05-08/protected-anchor-service-feature-gate-two-iteration-online-decision-wide-replay-roll-forward-4x0.2/` from the two-iteration feature-gate online scorecard for `lns-holdout-anchor-service-shelf-pressure`, seeds `7`, `19`, and `37`. It captures the 6 anchor-service online decision states after the useful first override and the following fallback state, expands to 24 replay windows per state (`max-windows=16`, `exploration-windows=8`), and rolls each label forward for 4x0.2s. All 144 roll-forward labels finish neutral versus the deterministic baseline: the selected first `weak-service->random-exploration` window still has immediate repair delta `+110`, but its 4-step final delta is `0`, and every post-first-repair `random-exploration->random-exploration` fallback state has best final delta `0`. This closes the current durable-follow-up branch for the existing anchor-service shelf opportunity; the baseline catch-up is not just a shallow two-iteration artifact under wider replay coverage.
- The anchor-service shelf repeatability replay `protected-anchor-service-shelf-repeatability-wide-state-search-roll-forward-4x0.2` is generated under `artifacts/lns-window-replay-labels/2026-05-08/` using 9 seeds (`5`, `7`, `11`, `13`, `17`, `19`, `23`, `29`, `37`), initial-incumbent and post-stagnation state policies, `max-windows=16`, `exploration-windows=8`, and 4x0.2s roll-forward. It captures 18 states and 432 labels with no repeatability conflicts: 48/48 buckets repeated, 0 mixed buckets, 0 conflicting labels, and 0 feature-identical conflicts. The immediate repair opportunity is fully repeatable but not durable: 54 labels improve immediately by `+110` (`frontier-congestion`, `gate-choke`, and `random-exploration` on both state policies for every seed), yet 432/432 roll-forward labels are final-neutral because the deterministic baseline also reaches `690` in every seed/state. This confirms the known anchor-service shelf lift is robust trajectory value only; it is not a hidden durable final-population promotion source.
- The second protected anchor-service case was searched for durable opportunity in `protected-anchor-service-harbor-wide-state-search-roll-forward-4x0.2` under `artifacts/lns-window-replay-labels/2026-05-08/` using initial-incumbent, post-first-improvement, and post-stagnation state policies, seeds `7`, `19`, and `37`, `max-windows=16`, `exploration-windows=8`, `repair-time=0.2`, and 4x0.2s roll-forward. It produced 6 captured states and 144 labels, but no usable opportunity: 0 positive immediate repairs, 0 positive final-lift labels, and 144/144 roll-forward labels final-neutral. Only initial-incumbent and post-stagnation states materialized because state collection found no first improvement. This rules out the broadened anchor-service harbor case as the next durable protected lift source.
- The non-anchor active-override cases were searched in `protected-gate-service-wide-state-search-roll-forward-4x0.2` under `artifacts/lns-window-replay-labels/2026-05-08/` for `lns-holdout-gate-double-choke-pressure` and `lns-holdout-service-braid-pressure`, seeds `7`, `19`, and `37`, the same wide-window/state policies, and 4x0.2s roll-forward. The artifact has 13 captured states, 312 labels, and all three state policies represented. Gate stays fully neutral: 144/144 labels final-neutral. Service-braid exposes a new protected roll-forward-only opportunity but not a safe policy yet: 29/168 service-pressure labels finish improved with best final delta `+50`, 119 finish neutral, and 20 regress by `-50`; all immediate repair deltas are `0`. The positive labels come from non-baseline-selected windows on initial-incumbent/post-stagnation states for seeds `7` and `19`, while the same window family regresses on seed `37` before its post-first-improvement state catches up to neutral. This is the first durable non-anchor protected signal, but it must be treated as a seed-sensitive selectivity target, not a solver-default promotion candidate.
- The service-braid seed-sensitivity diagnosis shows this signal is not separable by the current static replay feature payload. For the initial-incumbent and post-stagnation states, 20 state/operator/window keys flip between positive and regressed outcomes across seeds, and all 20 have identical feature fingerprints. Seeds `7` and `19` keep the baseline continuation at `760` while selected non-baseline windows can roll forward to `810`; seed `37` flips the comparison because the baseline `weak-service:2,0:3x3` continuation reaches `810` while those alternate windows stay at `760`. The seed `37` post-first-improvement state starts from `810` and is fully neutral. Future work should therefore treat service-braid as a stochastic/trajectory-variance calibration target requiring repeatability or baseline-success features, not as a safe feature-gate candidate under the current schema.
- The focused service-braid repeatability sweep `protected-service-braid-repeatability-wide-state-search-roll-forward-4x0.2` expands the same initial-incumbent/post-stagnation search to 9 seeds (`5`, `7`, `11`, `13`, `17`, `19`, `23`, `29`, `37`) with 432 labels. It confirms the service-braid signal is net unsafe under simple seed aggregation: 47 labels finish improved, 311 neutral, and 74 regressed, with all non-neutral deltas at `+50` or `-50`. Baseline continuation success is the controlling variable: seeds `17`, `29`, and `37` reach baseline `810` in both states and create 20 regressions each; seed `11` reaches baseline `810` only post-stagnation and contributes 14 regressions; seeds `7`, `13`, and `19` produce selected-window wins while baseline remains at `760`; seeds `5` and `23` stay fully neutral. The 20 flip buckets remain feature-identical across all 9 seeds, so the current replay feature schema cannot promote these windows safely without adding repeatability/baseline-success evidence or changing the label target.
- The remaining broadened second-case families were searched in `protected-corridor-footprint-wide-state-search-roll-forward-4x0.2` under `artifacts/lns-window-replay-labels/2026-05-08/` for `lns-holdout-corridor-switchback-pressure` and `lns-holdout-footprint-lattice-pressure`, seeds `7`, `19`, and `37`, all state policies, and the same wide-window/4x0.2 roll-forward settings. The artifact has 15 captured states and 360 labels. Corridor is fully neutral: 144/144 labels finish final-neutral with baseline and selected totals fixed at `560`. Footprint-lattice finds no positive final-lift labels and adds safety evidence against broad alternates: 54 initial-incumbent labels regress by `-60` because the baseline reaches `740` while alternate windows stay at `680`; post-first-improvement and post-stagnation are fully neutral. This closes the simple protected second-case search branch: the only non-anchor durable signal found is service-braid, and that signal is net unsafe under repeatability.
- The first four broadened protected holdout cases were screened for hidden durable state-search opportunity in `protected-first-broadened-cases-wide-state-screen-roll-forward-2x0.1` under `artifacts/lns-window-replay-labels/2026-05-09/`. The screen covers `lns-holdout-corridor-weave-pressure`, `lns-holdout-gate-offset-pressure`, `lns-holdout-footprint-stagger-pressure`, and `lns-holdout-service-ridge-pressure`, seeds `7`, `19`, and `37`, initial-incumbent plus post-stagnation states, `max-windows=8`, `exploration-windows=4`, and 2x0.1s roll-forward. It captures 24 states and 288 labels with clean repeatability (`96/96` repeated buckets, 0 mixed/conflicting buckets, and 0 feature-identical conflicts). All 288 roll-forward labels finish final-neutral versus the deterministic baseline. Corridor-weave has 18 immediate `+65` repairs, but baseline reaches the same final population in roll-forward; gate-offset, footprint-stagger, and service-ridge are fully neutral. This does not warrant full-depth validation and narrows the next protected generation work to new case construction rather than more replay over the existing first-case holdout set.
- The first handcrafted protected case-construction probe `protected-service-braid-range1-big-service120-candidate-wide-state-search-roll-forward-4x0.2` is generated under `artifacts/lns-window-replay-labels/2026-05-09/`. It mutates `lns-holdout-service-braid-pressure` by reducing the large service range to `1` and raising its bonus to `120`. The quick initial-state screen looked promising across seeds `7`, `19`, and `37`, but the full-depth wide state search blocks the candidate: 216 labels finish with 11 improved, 13 regressed, and 192 neutral labels, with best final delta `+25` and worst final delta `-90`. The positive labels occur only on seed `7`; seeds `19` and `37` regress on initial-incumbent/post-stagnation states when the deterministic baseline reaches `810` and alternate windows roll forward to `720` or `755`. Repeatability has 88 buckets with 17 mixed-status buckets, 0 conflicting buckets, and 0 feature-identical conflicts, so this branch is useful generator-search diagnostics but not a protected promotion candidate.
- Two nearby handcrafted service-braid range/bonus probes were generated after the blocked `120`-bonus candidate. The closer near miss `protected-service-braid-range1-big-service140-candidate-wide-state-search-roll-forward-4x0.2` has 216 labels, 23 improved, 4 regressed, and 189 neutral labels, with small non-neutral deltas (`+5` best, `-5` worst), 7 mixed repeatability buckets, and 0 feature-identical conflicts. It is still blocked because seed `19` post-first-improvement contributes all 4 regressions when its baseline reaches `850`. The attempted safety backoff `protected-service-braid-range1-big-service135-candidate-wide-state-search-roll-forward-4x0.2` is worse: 11 improved, 7 regressed, 198 neutral, best `+55`, worst `-90`, and 2 feature-identical repeatability conflict buckets. The service-braid construction branch therefore has a bounded near miss but no safe durable candidate; the next generator pass needs to avoid post-first baseline-success pockets or introduce a feature/state signal that can identify them.
- The state-gated repeatability follow-up `protected-service-braid-range1-big-service140-initial-stagnation-repeatability-wide-state-search-roll-forward-4x0.2` is generated under `artifacts/lns-window-replay-labels/2026-05-10/` with 9 seeds (`5`, `7`, `11`, `13`, `17`, `19`, `23`, `29`, `37`), initial-incumbent and post-stagnation states only, `max-windows=16`, `exploration-windows=8`, and 4x0.2s roll-forward. It improves the service-braid shape but does not clear promotion safety: 432 labels finish with 62 improved, 2 regressed, and 368 neutral labels, best `+5`, worst `-50`, with 4 mixed repeatability buckets and 0 conflicting or feature-identical conflict buckets. The regressions are narrow (`placed-buildings 1,4:3x3` on seed `19` post-stagnation and `sliding 2,3:3x3` on seed `23` post-stagnation), while the `selectivity-summary` shows an existing-feature diagnostic gate of `sliding` plus `area=12` selects 54/54 positive labels, 0 neutral, and 0 regressed labels across all 9 seeds. This is the first clean feature-separable durable pocket in the constructed service-braid branch, but it remains artifact-level evidence until tested against broader protected cases and an online-selectivity path.
- LNS window-replay artifact bundles now include first-class repeatability summaries. Replay text, manifests, and `repeatability-summary.json` group roll-forward labels by case, state policy, operator, and repair window; count repeated/mixed/conflicting final-status buckets; and flag conflicts whose feature fingerprints are identical. This turns the service-braid flip pattern from a manual diagnosis into a reusable artifact guardrail for future label/model targets.
- Offline LNS window-ranker baseline and training gates now consume repeatability summaries. Source label snapshots with feature-identical repeatability conflicts fail the offline promotion gate unless the run explicitly quarantines those buckets with `--exclude-feature-identical-repeatability-conflicts` / `--exclude-repeatability-conflicts`; telemetry, registry drafts, model snapshots, and text summaries record raw conflict counts plus excluded label/decision counts. This makes the service-braid failure mode a promotion-readiness rule, not just a post-hoc artifact note.
- Offline LNS ranker targets now include `roll-forward-baseline-stall-lift` via `--roll-forward-baseline-stall-lift` / `--baseline-stall-target`. This diagnostics-only target rewards alternate windows only when the deterministic baseline continuation does not improve from the incumbent; if the baseline continuation already succeeds, the decision contributes no positive lift signal. It gives the service-braid repeatability conflict an explicit baseline-continuation interpretation without adding a runtime feature or changing solver defaults.
- The first real baseline-stall offline bundles are generated from the strict curated non-weak roll-forward label source. The learned ranker `strict-lns-replay-labels-curated-seeds-v3-roll-forward-1x0.1-baseline-stall-no-weak-cpu-ranker-lr0.02-cap100` is under `artifacts/lns-window-ranker/2026-05-08/` with target `roll-forward-baseline-stall-lift`, label fingerprint `fnv1a:58ce487f`, model fingerprint `fnv1a:0c8ed61a`, 45 development decisions, 45 holdout decisions, 48 opportunities, and `passed=false`: holdout capture `0.5185` trails the best stable-random baseline `0.5679` by `-0.0494` despite hit@1 `0.6` and hit@K `1`. The paired baseline sweep `strict-lns-replay-labels-curated-seeds-v3-roll-forward-1x0.1-baseline-stall-no-weak-baselines` is under `artifacts/lns-window-ranker-baselines/2026-05-08/` and confirms stable-random is the best baseline for this target, with deterministic capture `0`. This closes the baseline-stall branch as a negative diagnostics result: it is useful interpretation for baseline-success conflicts, but it is weaker than the current final-lift feature-gated path and does not warrant protected online promotion testing.
- The broader static feature-gate replay scan `protected-sliding-area12-existing-replay-safety-scan` is generated under `artifacts/lns-window-replay-feature-gate-scans/2026-05-10/` using `scripts/summarize-lns-replay-feature-gate-scan.mjs`. It scans 19 existing protected replay artifacts, 14 cases, 9 seeds, and 4,068 roll-forward labels. The constructed `sliding && area=12` pocket is not globally safe: `slidingArea12` selects 401 labels with 94 improved, 281 neutral, and 26 regressed outcomes, best `+55`, worst `-60`. All nearby static gates are also unsafe (`slidingArea12ComponentsMax2`: 20 regressions, worst `-50`; `slidingTopOrLeft2`: 28 regressions, worst `-60`; all non-baseline sliding: 47 regressions, worst `-60`). This converts the state-gated service-braid pocket into a bounded local construction signal, not a broad protected feature gate or online-selectivity promotion candidate.
- The compact systematic service-braid neighbor screen `protected-service-braid-range1-big-service138-142-initial-stagnation-screen-roll-forward-2x0.1` is generated under `artifacts/lns-window-replay-labels/2026-05-10/` via `scripts/generate-service-braid-replay-artifacts.mjs --bundle=service-braid-range1-big-service138-142-initial-stagnation-screen`. It tests bonuses `138`, `140`, and `142` with range `1`, seeds `7`, `19`, and `37`, initial-incumbent plus post-stagnation states, `max-windows=8`, `exploration-windows=4`, and 2x0.1s roll-forward. The screen confirms the construction is narrow: bonus `138` is unsafe with 16 improved, 12 regressed, and 44 neutral labels (worst `-48`) plus 12 feature-identical repeatability conflict buckets; bonus `140` is the only positive safe neighbor with 54 improved, 0 regressed, 18 neutral labels, best `+60`, worst `0`; bonus `142` is fully neutral. The local `slidingArea12` gate stays non-regressing in this compact band (10 improved, 8 neutral, 0 regressed), but the broader existing-replay scan still blocks it globally, so no online/default promotion follows from this screen.
- The original-range service-braid bonus screen `protected-service-braid-range2-big-service120-160-initial-stagnation-screen-roll-forward-2x0.1` is generated under `artifacts/lns-window-replay-labels/2026-05-10/` via `scripts/generate-service-braid-replay-artifacts.mjs --bundle=service-braid-range2-big-service120-160-initial-stagnation-screen`. It keeps the large service at range `2`, tests bonuses `120`, `140`, and `160`, and uses the same compact seeds/state gate. This axis is fully neutral: 216/216 labels are final-neutral, 0 immediate positives, 0 regressions, 0 mixed buckets, and 0 feature-identical conflicts. The `slidingArea12` and all non-baseline sliding gates are also fully neutral. Raising the original-range service bonus therefore does not reproduce the `range=1/bonus=140` durable pocket and should not receive full-depth replay.
- The residential-pressure service-braid screen `protected-service-braid-residential-headroom-initial-stagnation-screen-roll-forward-2x0.1` is generated under `artifacts/lns-window-replay-labels/2026-05-10/` via `scripts/generate-service-braid-replay-artifacts.mjs --bundle=service-braid-residential-headroom-initial-stagnation-screen`. It keeps the original service ranges and tests three residential-pressure mutations: roughly `+20%` max headroom, roughly `+40%` max headroom, and a premium-scarcity variant with the 2x3 minimum raised to `220` and max `560`. This axis is also fully neutral: 216/216 labels final-neutral, 0 immediate positives, 0 regressions, 0 mixed buckets, and 0 feature-identical conflicts. Residential headroom alone therefore does not induce a durable service-braid pocket, and the next construction search should change geometry/state collection rather than scalar service or residential values.
- The simple blocker-geometry service-braid screen `protected-service-braid-geometry-initial-stagnation-screen-roll-forward-2x0.1` is generated under `artifacts/lns-window-replay-labels/2026-05-10/` via `scripts/generate-service-braid-replay-artifacts.mjs --bundle=service-braid-geometry-initial-stagnation-screen`. It keeps the original service and residential values while opening the upper central braid blocker, opening the lower-left notch blocker, and shifting the right-side choke one cell inward. This first geometry axis is fully final-neutral: 216/216 labels final-neutral, 9 immediate positives, 0 regressions, 0 mixed buckets, and 0 feature-identical conflicts. The static sliding gates are likewise neutral (`slidingArea12`: 18 neutral; all non-baseline sliding: 54 neutral), so small allowed-cell edits around the existing braid do not reproduce the durable `range=1/bonus=140` pocket.
- The structural range1/bonus140 service-braid screen `protected-service-braid-range1-big-service140-structural-geometry-screen-roll-forward-2x0.1` is generated under `artifacts/lns-window-replay-labels/2026-05-10/` via `scripts/generate-service-braid-replay-artifacts.mjs --bundle=service-braid-range1-big-service140-structural-geometry-screen`. It tests the known local `range=1/bonus=140` setting across three larger topology changes: an added southern shelf row, an added eastern lane, and a stronger central double choke. The compact screen has 216 labels with 4 improved, 0 regressed, and 212 neutral outcomes, best `+55`, worst `0`, and 0 feature-identical conflicts. All final positives come from the southern-shelf variant on seeds `19` and `37`; east-lane and double-choke are final-neutral. This made southern shelf worth a repeatability rerun, but it is not standalone promotion evidence.
- The southern-shelf repeatability rerun `protected-service-braid-range1-big-service140-south-shelf-repeatability-wide-state-search-roll-forward-4x0.2` is generated under `artifacts/lns-window-replay-labels/2026-05-10/` with 9 seeds, initial-incumbent plus post-stagnation states, `max-windows=16`, `exploration-windows=8`, and 4x0.2s roll-forward. It blocks the structural candidate: 432 labels finish with 0 improved, 1 regressed, and 431 neutral outcomes, best `0`, worst `-55`, 1 mixed repeatability bucket, and 0 feature-identical conflicts. The lone regression is seed `13` post-stagnation, where the deterministic baseline reaches `850` and the selected sliding window remains at `795`; the local `slidingArea12` gate also catches this regression (`0` improved, `1` regressed, `35` neutral). Treat the southern-shelf compact positives as shallow-budget artifacts, not a durable protected pocket.
- The generated-grid service-braid family screen `protected-service-braid-generated-grid-family-screen-roll-forward-2x0.1` is generated under `artifacts/lns-window-replay-labels/2026-05-10/` via `scripts/generate-service-braid-replay-artifacts.mjs --bundle=service-braid-generated-grid-family-screen`. It procedurally embeds the original blocker pattern into 7x8 boards at origin, down-shift, right-shift, and mirrored offsets while keeping the original service and residential values. The compact screen has 288 labels with 6 improved, 0 regressed, and 282 neutral outcomes, best `+50`, worst `0`, and no mixed or feature-identical repeatability conflicts. All positives come from the right-shifted generated board; the origin, down-shift, and mirror variants are final-neutral. This gives a non-hand-tuned family hit, but only at compact-screen depth.
- The right-shift generated-family repeatability rerun `protected-service-braid-generated-grid-family-right-repeatability-wide-state-search-roll-forward-4x0.2` is generated under `artifacts/lns-window-replay-labels/2026-05-10/` with 9 seeds, initial-incumbent plus post-stagnation states, `max-windows=16`, `exploration-windows=8`, and 4x0.2s roll-forward. It finds real signal but still blocks raw candidate promotion: 432 labels finish with 22 improved, 2 regressed, and 408 neutral outcomes, best `+50`, worst `-45`, 4 mixed repeatability buckets, 2 conflicting buckets, and 2 feature-identical conflict buckets. The regressions are both seed `7` on `random-exploration 3,1:3x3`; the local `slidingArea12` gate remains safe in this artifact with 20 improved, 0 regressed, and 16 neutral labels. Because the broader existing-replay scan already proved static `slidingArea12` unsafe globally, this is a state/selectivity-mechanism lead rather than a promotion candidate: future work should separate the safe generated-family sliding pocket from the feature-identical random-exploration conflicts and from the global static-gate failures.
- The repeatability-aware feature-gate scan `protected-repeatability-aware-existing-replay-safety-scan` is generated under `artifacts/lns-window-replay-feature-gate-scans/2026-05-10/` via the updated `scripts/summarize-lns-replay-feature-gate-scan.mjs`. It scans the current replay-label tree: 27 artifacts, 35 cases, 9 seeds, and 6,300 roll-forward labels. The raw static `slidingArea12` gate remains unsafe with 587 selected labels, 134 improved, 27 regressed, 426 neutral, best `+60`, and worst `-60`. Excluding only feature-identical repeatability conflicts is still unsafe: 539 selected, 120 improved, 9 regressed, 410 neutral, worst `-60`. The diagnostics-only repeatability-safe bucket gate is clean: 144 selected labels across 7 artifacts, 120 improved, 0 regressed, 24 neutral, best `+60`, worst `0`. This is not a runtime gate because it uses replay outcome buckets, but it proves the next viable path is repeatability-aware/state-conditioned selectivity rather than another static feature predicate.
- The runtime-observable selectivity scan `protected-runtime-observable-selectivity-scan` is generated under `artifacts/lns-window-replay-feature-gate-scans/2026-05-10/` via the same feature-gate scan script. It keeps the replay-outcome oracle for comparison but adds diagnostics-only gates built only from state/window features that are observable before choosing an LNS repair. The simple high-pressure gate `slidingArea12HighServiceCandidatePressure` (`serviceCandidateBonusInside >= 2790` and `residentialCandidatesBlockedByIncumbent >= 13`) selects 147 labels with 88 improved, 0 regressed, 59 neutral, best `+60`, worst `0`; 108 of those labels overlap the repeatability-safe bucket, preserving all 88 improved labels in that overlap. The narrower `slidingArea12ServiceComponentPocket` selects 63 labels with 58 improved, 0 regressed, 5 neutral, and 60 repeatability-safe overlaps. The screened `slidingArea12ObservablePressureEnsemble` selects 173 labels with 116 improved, 0 regressed, 57 neutral, best `+60`, worst `0`, and overlaps 138 of the 144 repeatability-safe bucket labels, including 116/120 of its improved labels. This is still diagnostics-only and deliberately not a solver default change, but it converts the repeatability-safe oracle into a near-matching observable feature-value candidate and motivates online diagnostic support for absolute selected-window feature gates.
- Online selected-feature gates are now implemented as diagnostics-only ranker controls through `lns.windowRanker.selectedFeatureGates` and `lnsBenchmarkCli --window-ranker-selected-feature-gates=<feature>=...` / `--window-ranker-feature-value-gates=<feature>=...`. Runtime validation, window-ranker fallback telemetry, solver telemetry, online ablation artifacts, registry drafts, text formatting, and tests now preserve these absolute selected-window thresholds without changing solver defaults. The same path now also supports disjunctive selected-feature groups through `lns.windowRanker.selectedFeatureGateGroups` and `--window-ranker-selected-feature-gate-groups=<group;group>` / `--window-ranker-feature-value-gate-groups=<group;group>`; each group is an AND of feature thresholds, and the groups are ORed. The protected online diagnostic `protected-broadened-one-iteration-selected-feature-gate-high-service-pressure-min-score-delta-1.18` is generated under `artifacts/lns-window-ranker-online/2026-05-10/` with the current protected broadened one-iteration corpus, seeds `7`, `19`, and `37`, model fingerprint `fnv1a:c198992d`, `minScoreDelta=1.18`, and normalized gates `area>=0.6`, `area<=0.6`, `serviceCandidateBonus>=5.58`, and `residentialCandidatesBlocked>=0.65`. The gate is safe but not valuable online: 30/30 ranker decisions fall back to the baseline (`weak-service->weak-service`), 0 overrides, 0 layout changes, mean delta `0`, worst delta `0`, 0 improvements, 0 regressions, and 30 unchanged comparisons; equal-population time-to-best passes, but the promotion gate still fails. This means the observable high-pressure replay pocket is not being nominated by the current online model/corpus, so promotion work should diagnose model/case coverage before adding broader ensemble gates.
- The focused service-braid online decision replay `protected-service-braid-online-selected-feature-gated-decision-coverage-roll-forward-4x0.2` is generated under `artifacts/lns-window-replay-labels/2026-05-10/` from the all-fallback selected-feature-gated online scorecard, restricted to `lns-holdout-service-braid-pressure`, seeds `7`, `19`, and `37`, `max-windows=16`, `exploration-windows=8`, and 4x0.2s roll-forward. It captures 3 online states and 72 labels. This explains the all-fallback selected-feature result: the original protected service-braid online states contain 9 non-baseline `sliding && area=12` labels, but the high-pressure, component-pocket, and observable-ensemble gates all select `0` labels because service-candidate bonus and blocked-residential pressure stay below the replay-scan thresholds. The available `slidingArea12` labels are unsafe and seed-sensitive instead: 4 improved, 2 regressed, 3 neutral, best `+50`, worst `-50`, with the regressions on seed `37`; the replay also has 10 repeatability conflicts and 10 feature-identical conflict buckets. The runtime-observable pressure pocket therefore lives in the generated/mutated service-braid cases, not in the current original protected service-braid online state. The next useful branch is coverage/model work that can bring those generated-family pockets into protected online nomination, not a broader online ensemble gate over the current corpus.
- `scripts/generate-service-braid-replay-artifacts.mjs` now has a diagnostics-only `--online-ablation` mode that runs the custom generated service-braid cases through the online LNS window-ranker scorecard path and emits scorecard JSON/text, telemetry, registry draft, and manifest artifacts. On the right-shift generated-family repeatability candidate, the current protected model `fnv1a:c198992d` at `minScoreDelta=1.18` remains all-fallback (`9` decisions, `0` overrides, mean delta `0`), and the ignore-baseline model `fnv1a:0703fa5c` with the high-pressure selected-feature gate is also all-fallback at `minScoreDelta=0`. The selected good window from `fnv1a:0703fa5c` has `area=0.6`, `serviceCandidateBonus=4.9`, `residentialCandidatesBlocked=0.75`, `emptyComponentsBefore=0.3`, and `operatorScore=9.74`, so it misses the high-pressure `serviceCandidateBonus>=5.58` conjunction but matches the observable component/operator branch.
- The component/operator proxy selected-feature gate (`area>=0.6`, `area<=0.6`, `emptyComponentsBefore>=0.3`, `emptyComponentsBefore<=0.3`, `operatorScore<=9.75`) unlocks the right-shift generated-family candidate with the ignore-baseline model while staying inert on the broadened protected corpus. The one-iteration generated artifact `service-braid-generated-grid-family-right-repeatability-ignore-baseline-component-proxy-gate-min-score-delta-0` improves 9/9 seeds with mean/worst delta `+50`, 0 regressions, and 9/9 overrides. The two-iteration generated artifact `service-braid-generated-grid-family-right-repeatability-ignore-baseline-component-proxy-gate-two-iteration-min-score-delta-0` keeps the same durable final lift (`+50` mean/worst, 9/9 improved, 0 regressions) with 9 overrides followed by 9 fallbacks. The protected broadened one- and two-iteration counterparts `protected-broadened-one-iteration-ignore-baseline-component-proxy-gate-min-score-delta-0` and `protected-broadened-two-iteration-ignore-baseline-component-proxy-gate-min-score-delta-0` are safe all-fallback scorecards: mean delta `0`, worst delta `0`, 0/30 regressions, and 0 overrides across 30 and 60 ranker decisions respectively. This is promising coverage/model evidence for generated service-braid pockets, but still diagnostics-only because the useful case is generated and the gate is hand-selected rather than learned promotion policy.
- The disjunctive observable proxy gate combines the high-pressure branch (`area=0.6`, `serviceCandidateBonus>=5.58`, `residentialCandidatesBlocked>=0.65`) with the component/operator branch (`area=0.6`, `emptyComponentsBefore=0.3`, `operatorScore<=9.75`) for the ignore-baseline model `fnv1a:0703fa5c`. On the original right-shift generated-family candidate, `service-braid-generated-grid-family-right-repeatability-ignore-baseline-observable-proxy-gate-min-score-delta-0` improves 9/9 seeds with mean/worst delta `+50`, 0 regressions, 9 decisions, and 9 overrides. The two-iteration companion keeps the same final lift with 18 decisions, 9 overrides, and 9 fallbacks. The protected broadened one- and two-iteration counterparts are safe all-fallback scorecards: mean delta `0`, worst delta `0`, 0/30 improvements, 0/30 regressions, and 0 overrides over 30 and 60 decisions. This verifies the OR-group plumbing and keeps the generated service-braid pocket isolated from the current protected broadened corpus, but it is still hand-gated diagnostics.
- The generated right-neighborhood online screen broadens the right-shift hit to six generated service-braid variants around the same area. The observable proxy gate remains safe and positive only on the anchor: `service-braid-generated-grid-right-neighborhood-ignore-baseline-observable-proxy-gate-min-score-delta-0` has mean delta `+8.333`, worst `0`, 3/18 improved, 0/18 regressed, 3 overrides, and 15 fallbacks; the two-iteration companion keeps the same population deltas with 3 overrides and 33 fallbacks. All lift comes from the anchor candidate seeds, while the five neighbor variants are unchanged fallback rows. The replay-label version of this neighborhood screen was stopped before producing artifact files because it exceeded practical runtime, so this pass is an online coverage diagnostic rather than label-corpus evidence.
- The ignore-baseline model without selected-feature gates is unsafe even on the generated right-neighborhood screen. `service-braid-generated-grid-right-neighborhood-ignore-baseline-no-selected-feature-gate-min-score-delta-0` ties mean delta at `0` only by mixing 3 improvements with 3 regressions, worst `-50`, and 18/18 overrides. The two-iteration no-gate companion is worse: mean delta `-5.556`, worst `-50`, 3/18 improved, 5/18 regressed, and 36/36 overrides. This closes the "no manual proxy gate" branch negatively for the current model; grouped selected-feature gates are necessary guardrails rather than optional presentation polish.
- Online selected-feature gate discovery now exists as a repeatable diagnostics artifact path through `scripts/discover-lns-online-selected-feature-gates.mjs`. The discovery artifact `right-neighborhood-ignore-baseline-no-gate-selected-feature-discovery` is generated under `artifacts/lns-window-ranker-online-gate-discovery/2026-05-10/` from the unsafe generated right-neighborhood no-gate one- and two-iteration scorecards. It scans 54 override traces, including 6 immediate-improving selections and 13 final-regressed selections, and discovers the greedy safe group `allowedWindowCells>=0.5`: 6 selected, 6 immediate improved, 0 final regressed, 0 neutral. The top safe alternatives include `area>=0.6`, `clearedFootprint>=0.35`, `newlyReachable>=0.16`, and `serviceCandidateBonus>=4.9`, confirming the hand-picked component/operator proxy is recoverable from online trace data rather than only manual inspection.
- The discovered `allowedWindowCells>=0.5` gate reproduces the generated anchor lift and passes the broadened protected population safety recheck, but it does not promote. On the generated right-neighborhood screen, `service-braid-generated-grid-right-neighborhood-ignore-baseline-discovered-allowed-window-gate-min-score-delta-0` has mean delta `+8.333`, worst `0`, 3/18 improved, 0 regressions, 3 overrides, and 15 fallbacks; the two-iteration companion has the same final population deltas with 3 overrides and 33 fallbacks. On the protected broadened corpus, the one-iteration recheck has mean `0`, worst `0`, and 0/30 regressions but allows 3 final-neutral `weak-service->sliding` overrides; the two-iteration recheck is also population-safe with mean `0`, worst `0`, and 0 regressions, but allows 9 final-neutral overrides and changes 2 final layouts. This is learned/discovered selectivity evidence for the generated pocket, not default-promotion evidence.
- The generated right value/range neighborhood screen `service-braid-generated-grid-right-value-neighborhood-screen` broadens the anchor through 10 scalar mutations: large-service bonus `85/105/120`, range `1/3`, range1+bonus120, residential headroom, premium scarcity, and two nearby generated geometries with value changes. With the discovered `allowedWindowCells>=0.5` gate, both one- and two-iteration online scorecards are all-fallback: mean delta `0`, worst `0`, 0 improvements, 0 regressions, and 0 overrides across 30 comparisons. The no-gate one-iteration diagnostic is also fully final-neutral despite 30/30 overrides, with mean `0`, worst `0`, 0 improvements, and 0 regressions. This closes the value/range broadening branch for now: there is no non-anchor lift worth spending replay-label budget on.
- The generated right geometry neighborhood screen `service-braid-generated-grid-right-geometry-neighborhood-screen` broadens the anchor through 12 topology variants around the right-shift pattern: upper caps, double blockers, outer chokes, 8x8 bottom shelves, and 7x9 west-lane shifts. With the ignore-baseline model and no selected-feature gate, the one-iteration scorecard is fully final-neutral despite 36/36 overrides, while the two-iteration companion is unsafe: mean delta `-26.389`, worst `-100`, and 16/36 regressions. With the discovered `allowedWindowCells>=0.5` gate, both one- and two-iteration runs are all-fallback safe with mean `0`, worst `0`, 0 improvements, 0 regressions, and 0 overrides across 36 comparisons. This closes the simple geometry broadening branch: none of the 12 variants creates non-anchor lift worth spending replay-label budget on.
- A generated-repeatability model-target diagnostic now adds the right-shift generated service-braid repeatability replay as supplemental final-lift evidence while excluding feature-identical repeatability conflicts and ignoring `selectedByBaseline` for neutral supplemental calibration pairs. The ranker artifact `strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-generated-right-repeatability-ignore-baseline-conflict-excluded-calibration-lr0.02-cap100` is generated under `artifacts/lns-window-ranker/2026-05-10/` with model fingerprint `fnv1a:1bcfef75`, 87 supplemental replay decisions, 939 supplemental replay labels, 18 excluded conflict labels, and offline holdout capture `0.4615` versus the fragmentation baseline `0.5726`; it is diagnostics-only and fails the offline gate. Online, the no-selected-feature-gate generated right-neighborhood run recovers anchor lift at one iteration (`+8.333` mean, worst `0`, 3/18 improved), but the protected one-iteration 3-seed recheck regresses footprint-lattice (`-6` mean, worst `-60`, 3/30 regressions). A pure score threshold at `0.93` suppresses that one-iteration protected regression while keeping the generated anchor, but the default two-iteration path fails both generated and protected screens: generated right-neighborhood falls to mean `+2.778`, worst `-50`, 2/18 regressions, and protected broadened falls to mean `-15.5`, worst `-110`, 6/30 regressions. The second-iteration protected regressions score above the useful generated first repair, so a scalar ranker threshold cannot replace selected-feature/state guardrails for default-trajectory promotion.
- The trajectory-aware ranker feature spike is implemented and tested behind the opt-in `--trajectory-features` flag. Runtime and replay scoring now support baseline-operator, selected-operator, and baseline-to-selected transition one-hot features under ranker feature schema `3` while accepting existing schema `2` models. The calibrated artifact `strict-lns-replay-labels-curated-seeds-v3-final-lift-no-weak-generated-right-repeatability-trajectory-features-ignore-baseline-conflict-excluded-calibration-lr0.02-cap100` is generated under `artifacts/lns-window-ranker/2026-05-10/` with model fingerprint `fnv1a:09853445`, 107 features, and offline holdout capture `0.6154` versus fragmentation `0.5726`; it is still diagnostics-only because supplemental replay calibration blocks promotion. Online evidence is mixed and does not improve the promotion target. The generated right-neighborhood no-gate rechecks are unsafe at one iteration (`mean=-8.333`, worst `-50`, 3/18 regressions) and two iterations (`mean=-2.778`, worst `-50`, 5/18 regressions). The broadened protected one-iteration recheck is also unsafe (`mean=-12.5`, worst `-65`, 6/30 regressions). The broadened protected two-iteration recheck is population-safe and positive (`mean=+6`, worst `0`, 3/30 improved, 0 regressions) but overrides every decision and fails the promotion gate: `promotionBlocked=true`, `onlineOverrideCount=60`, `onlineFinalNeutralOverrideCount=54`, `offlinePositiveOnlineNeutralCount=1`, and `timeToBestPromotionGatePassed=false`. Trajectory one-hots improve offline fit but do not learn enough selectivity; this closes the simple trajectory-feature model-class branch as a default-promotion path.

Concrete work:

- Keep learned LNS ranking diagnostics-only because full protected exact online-state replay shows neutral reshuffles rather than final-lift.
- Use the full exact neutral calibration result as a guardrail for future LNS ranker work: a candidate model must keep protected overrides suppressed, or require a threshold high enough to suppress neutral protected overrides, without collapsing offline final-lift capture below the established baselines.
- Move the next solver roadmap focus from learned selectivity plumbing to promotion-grade value: the wide-window opportunity is now represented, the `0.1` threshold can select it online, one-iteration protected diagnostics show equal-budget lift, broader protected coverage is represented, manual transition and feature-delta gates can suppress the broadened neutral override family, the one-iteration final-lift promotion diagnostic clears, the default-trajectory time-to-best promotion axis is explicit, final-outcome feature gaps identify candidate selectivity signals, the broadened feature-gated trajectory remains safe through 4 iterations but still fails wall-clock time-to-best promotion, the supplemental pairwise-calibration replacement attempts are negative, the pairwise-interaction model-class spike improves offline fit but is unsafe both without the manual gate and under the current feature gate, both current anchor-service durable-follow-up paths are fully neutral, the expanded anchor-service shelf repeatability replay confirms robust but non-durable trajectory value, service-braid provides only a net-unsafe seed-sensitive durable signal, the remaining broadened second-case search is neutral or regressive, the first-case holdout state-search screen is all final-neutral, the handcrafted service-braid range/bonus construction is seed-split and blocked at full depth, the `140`-bonus state-gated repeatability run exposes a clean local `sliding && area=12` durable pocket, the broader existing-replay feature-gate scan blocks that pocket as a global static gate, the compact `138/140/142` neighbor screen confirms `140` is the only positive non-regressing nearby range-1 construction, the original-range `120/140/160` bonus screen, residential-headroom screen, and simple blocker-geometry screen are fully neutral, the structural south-shelf compact positives disappear under wide repeatability and add a `-55` regression, the generated-grid right-shift family produces repeatable sliding lift but raw labels are blocked by feature-identical random-exploration conflicts and the global static gate is already unsafe, the repeatability-safe bucket oracle keeps 120 improved labels with 0 regressions across current replay artifacts, the runtime-observable pressure ensemble approximates that oracle with 116 improved labels and 0 regressions, online selected-feature gates now carry absolute selected-window thresholds and grouped OR gates through validation, solver telemetry, CLI, artifacts, and registry drafts, online trace discovery can recover a safe selected-feature gate from unsafe no-gate runs, the first high-pressure selected-feature online diagnostic is all-fallback safe with no lift, the original protected service-braid online decision replay confirms the high-pressure pocket is absent there and the available sliding area-12 windows are seed-conflicted, the ignore-baseline model plus a disjunctive observable proxy or discovered `allowedWindowCells>=0.5` gate can now nominate the generated right-shift service-braid pocket online with durable `+50` lift while staying population-safe on broadened protected one- and two-iteration corpora, right-neighborhood generated coverage keeps only the anchor positive and leaves five nearby variants fallback-neutral, value/range and geometry generated broadening are neutral or unsafe, no-gate ignore-baseline testing is unsafe on generated coverage, generated-repeatability model calibration learns one-iteration anchor lift but fails default two-iteration safety, repeatability conflicts now feed artifact and ranker promotion gates, and the baseline-stall target is now a negative offline branch. Default-trajectory validation still fails both final-lift promotion and the new wall-clock time-to-best promotion gate because the deterministic baseline catches up after the selected repair and median time-to-best is not 10% faster. The next candidate needs a genuinely new state-conditioned durable source or model class with trajectory-aware suppression; scalar score thresholds and simple geometry/value mutations are exhausted for the current generated service-braid pocket.
- Keep planner-facing feature-delta explainability parked until a learned LNS ranker is exposed outside diagnostics-only benchmark flows.

Exit criteria:

- At least 5 pressure families in both development and holdout coverage.
- At least 200 usable labels and 50 non-neutral usable labels in each split.
- No pressure family with fewer than 20 usable labels.
- Neutral-label ratio below 85% in both development and holdout.
- Exact evaluator validation for every reported repaired layout.
- Registered artifact metadata includes command, git commit, hardware, split, budget, CP-SAT formulation/model fingerprint, and decision status.

### 2. Short-budget Auto gap triage

Impact: medium, evidence-only

Why:

- The promotion corpus shows Auto is broadly healthy, but a few 1s/5s rows still lose to standalone LNS or CP-SAT.
- These misses should be diagnosed before changing Auto stage budgets or default policies.

Delivered so far:

- Cross-mode budget ablations now support artifact bundles through `crossModeBenchmarkCli --budget-ablation --artifact-dir=...`. The bundle writes `budget-ablation.json`, `budget-ablation.txt`, `decision-trace.jsonl`, `telemetry-manifest.json`, and `registry-entry-draft.json`, so focused policy slices can be registered like the other benchmark evidence.
- The registered ablation-gate artifact `cross-mode-budget-ablation-2026-05-06-short-budget-auto-gap-triage-known-misses` is generated under `artifacts/cross-mode-budget-ablations/2026-05-06/short-budget-auto-gap-triage-known-misses/`. It covers the three known short-budget miss families (`expansion-comparison-replay`, `typed-footprint-pressure`, and `service-local-neighborhood`), budgets `1s` and `5s`, seeds `7`, `19`, and `37`, modes `auto`, `lns`, and `cp-sat`, and policies `baseline`, `repair-heavy`, and `cp-sat-reserve-heavy`, for 162 total mode runs and 486 budgeted mode-seconds.
- Baseline remains the top policy by Auto mean population. Baseline mean Auto population is `573.3` with mean Auto gap `13.056`; `repair-heavy` drops overall Auto mean by `-6.667`, and `cp-sat-reserve-heavy` drops it by `-9.444`.
- The miss shape is budget-specific. At `1s`, both heavier policies hurt Auto by `-18.889` mean population versus baseline, mainly by degrading seed quality on `service-local-neighborhood` and `expansion-comparison-replay`. At `5s`, `repair-heavy` improves Auto by `+5.556` and cuts mean Auto gap to `6.667`, while `cp-sat-reserve-heavy` ties baseline Auto mean but leaves LNS worse.
- The evidence does not support changing Auto defaults. It points instead to a possible future conditional policy: preserve the current 1s seed posture, then investigate a guarded 5s-only repair/CP-SAT handoff on the footprint and expansion-comparison misses with protected safety and CPU-efficiency reporting.
- Budget-ablation policy definitions now support optional `activeBudgetSeconds` and `appliesToCase` predicates, and optional policies can be selected by name without joining the default sweep. `baseline-repeat` repeats the current Auto/LNS budget policy as a variance control, `repair-heavy-5s-guarded` applies the combined guarded repair-heavy LNS allocation only at the 5s budget, the component/pairwise probes isolate the seed, repair-time, and CP-SAT-reserve pieces, and the conditional probes test runtime-visible case shapes. These are selectable through `--ablation-policies=...`, and none changes solver defaults. Budget-ablation summaries also now include `policyApplicationSummary` counters in JSON, text, telemetry manifests, and registry drafts so conditional-policy rows can be separated into applied versus inactive score drift.
- The registered guarded ablation artifact `cross-mode-budget-ablation-2026-05-06-short-budget-auto-gap-triage-guarded-5s-repair-heavy` is generated under `artifacts/cross-mode-budget-ablations/2026-05-06/short-budget-auto-gap-triage-guarded-5s-repair-heavy/`. It compares `baseline` against `repair-heavy-5s-guarded` on the same three miss families, budgets `1s` and `5s`, seeds `7`, `19`, and `37`, modes `auto`, `lns`, and `cp-sat`, for 108 total mode runs and 324 budgeted mode-seconds.
- In that guarded slice, `repair-heavy-5s-guarded` is the top policy by Auto mean population: mean Auto `577.5` versus baseline `573.6`, delta `+3.889`, and mean Auto gap `8.889` versus baseline `11.944`. At `5s`, guarded Auto mean is `582.8` versus baseline `579.4`, delta `+3.333`. The observed `1s` delta is `+4.444`, but the guarded policy is inactive at `1s`, so this is repeatability/wall-clock variance evidence rather than causal policy lift. The per-row diff has one observed guarded regression, `typed-footprint-pressure` at `5s` seed `37`, delta `-5`, so this remains diagnostics-only.
- Budget-ablation summaries now include paired Auto safety metrics against the resolved baseline policy: comparison count, mean/median/worst-decile/worst/best Auto deltas, regression count/rate, worst/best case/seed/budget labels, wall-clock delta, and CPU-budget efficiency ratio. These fields are preserved in JSON artifacts, text output, telemetry manifests, and registry drafts.
- Budget-ablation policy results now also carry row-level paired Auto replay diagnostics for nonzero baseline-vs-policy rows. The `autoReplayDiagnostics` payload records baseline/candidate final populations, wall-clock deltas, Auto stage summaries, LNS seed/repair/reserve settings, compact LNS neighborhood replay summaries from the final incumbent or preserved Auto LNS stage runs, and a compact reason string. Text output prints the same rows under `auto-replay-diagnostics`, while telemetry manifests and registry drafts preserve the structured payload.
- The product-corpus short-budget Auto-only paired safety preflight `cross-mode-budget-ablation-2026-05-06-product-corpus-short-budget-guarded-5s-repair-heavy-auto-safety` is generated under `artifacts/cross-mode-budget-ablations/2026-05-06/product-corpus-short-budget-guarded-5s-repair-heavy-auto-safety/`. It compares `baseline` and `repair-heavy-5s-guarded` across all 10 product-corpus cases, budgets `1s` and `5s`, seeds `7`, `19`, and `37`, mode `auto`, for 120 total Auto runs and 360 budgeted mode-seconds.
- The broader product-corpus preflight blocks the guarded policy as a default candidate: baseline remains top, while `repair-heavy-5s-guarded` has mean Auto delta `-1.333`, 5/60 paired Auto regressions, regression rate `0.083`, worst delta `-35`, mean wall-clock delta `+0.125s`, and CPU-budget efficiency ratio `0.996`. At `5s`, it has mean Auto delta `-1`, 3/30 regressions, regression rate `0.100`, worst-decile delta `-15`, and worst delta `-35`. This overturns the narrow three-family positive signal.
- The blocked-row trace separates repeatability noise from causal policy risk. The `1s` regressions on `service-local-neighborhood` seed `19` and `typed-footprint-pressure` seed `37` ran with the guarded policy inactive and identical parameters to baseline (`lns.seedTimeLimitSeconds=0.2`, no CP-SAT reserve); they still diverged because LNS found different late repairs (`+40` versus `+5` on service pressure, and `+15` versus `0` on footprint pressure). Treat these as evidence that future short-budget policy gates need baseline repeat/variance checks, not as proof of a `1s` policy effect.
- The observed product-corpus `5s` regressions are guarded-policy risk, but the row replay shows they are not equally stable. In the original product-corpus preflight, `typed-footprint-pressure` seeds `19` and `37` regressed because baseline found a late `+15` LNS repair to population `520`, while `repair-heavy-5s-guarded` shortened the LNS seed to `0.25s`, reserved CP-SAT time, and finished at `505`. In the focused row replay below, those two typed rows tied at `505` for both baseline and guarded, so treat the typed failures as variance-sensitive evidence rather than a reproducible tuning target.
- The registered row-level artifact `cross-mode-budget-ablation-2026-05-06-product-corpus-short-budget-guarded-5s-repair-heavy-row-diagnostics` is generated under `artifacts/cross-mode-budget-ablations/2026-05-06/product-corpus-short-budget-guarded-5s-repair-heavy-row-diagnostics/`. It compares `baseline` and `repair-heavy-5s-guarded` on `typed-footprint-pressure` and `expansion-comparison-replay`, budget `5s`, seeds `19` and `37`, mode `auto`, for 8 total Auto runs and 40 budgeted mode-seconds. Baseline remains the selected top policy by tie-break: guarded has mean Auto delta `0`, 1/4 improved rows, 1/4 regressed rows, worst delta `-35`, and best delta `+35`.
- The row replay isolates the stable expansion-comparison shape. On `expansion-comparison-replay` seed `37`, baseline reaches `780` by a third escalated `6x6` LNS replay window (`+35` after two neutral neighborhoods), while guarded reaches the same third window but stays neutral and finishes at `745`; the later CP-SAT stage adds `0`. On seed `19`, the sign flips: guarded finds the `+35` LNS repair to `780` while baseline stalls at `745`. This confirms the guarded policy is not a default candidate and that further Auto policy work needs repeat controls before attributing small paired deltas to budget settings.
- The registered variance-control artifact `cross-mode-budget-ablation-2026-05-06-product-corpus-short-budget-baseline-repeat-variance-1s-blocked-rows` is generated under `artifacts/cross-mode-budget-ablations/2026-05-06/product-corpus-short-budget-baseline-repeat-variance-1s-blocked-rows/`. It compares `baseline` and `baseline-repeat` on the two `1s` blocked rows (`service-local-neighborhood` and `typed-footprint-pressure`), seeds `19` and `37`, mode `auto`, for 8 total Auto runs and 8 budgeted mode-seconds. The repeat control changes no solver parameters, yet it improves `service-local-neighborhood` seed `19` by `+20` and has mean Auto delta `+5`, 0/4 regressions, and CPU-budget efficiency ratio `1.011`. This confirms that short-budget LNS variance is large enough to require repeat controls before interpreting small guarded-policy deltas.
- The registered 5s repeat-control artifact `cross-mode-budget-ablation-2026-05-06-product-corpus-short-budget-baseline-repeat-variance-5s-row-diagnostics` is generated under `artifacts/cross-mode-budget-ablations/2026-05-06/product-corpus-short-budget-baseline-repeat-variance-5s-row-diagnostics/`. It compares `baseline` and `baseline-repeat` on the same focused 5s row replay slice (`typed-footprint-pressure` and `expansion-comparison-replay`, seeds `19` and `37`, mode `auto`), for 8 total Auto runs and 40 budgeted mode-seconds. With identical solver parameters, `baseline-repeat` improves `typed-footprint-pressure` seed `19` by `+15` and seed `37` by `+20`, while both expansion-comparison rows remain unchanged. This tightens the interpretation: the typed 5s rows are repeat-sensitive LNS repair variance, while the expansion-comparison guarded-policy miss remains the cleaner policy-timing failure to inspect before any new Auto budget proposal.
- The registered triangulation artifact `cross-mode-budget-ablation-2026-05-06-product-corpus-short-budget-guarded-repeat-triangulation-5s-row-diagnostics` is generated under `artifacts/cross-mode-budget-ablations/2026-05-06/product-corpus-short-budget-guarded-repeat-triangulation-5s-row-diagnostics/`. It compares `baseline`, `baseline-repeat`, and `repair-heavy-5s-guarded` on the focused 5s typed/expansion row slice, for 12 total Auto runs and 60 budgeted mode-seconds. Guarded is top in this tiny slice with mean Auto delta `+3.75`, but it still regresses `typed-footprint-pressure` seed `19` by `-20` and wins only because `expansion-comparison-replay` seed `19` gains `+35`. Treat this as side-by-side diagnostic context, not promotion evidence.
- Budget-ablation summaries now compute an optional paired Auto variance envelope whenever `baseline-repeat` is present. The `autoVarianceSummary` compares each candidate row delta against the per-row baseline-repeat delta interval, preserves the result in JSON artifacts, text output, telemetry manifests, and registry drafts, and reports inside/outside counts plus the signed amount beyond the repeat envelope.
- The registered variance-aware triangulation artifact `cross-mode-budget-ablation-2026-05-06-product-corpus-short-budget-variance-aware-guarded-triangulation-5s-row-diagnostics` is generated under `artifacts/cross-mode-budget-ablations/2026-05-06/product-corpus-short-budget-variance-aware-guarded-triangulation-5s-row-diagnostics/`. It compares `baseline`, `baseline-repeat`, and `repair-heavy-5s-guarded` on the focused 5s typed/expansion row slice, for 12 total Auto runs and 60 budgeted mode-seconds. `baseline-repeat` stays fully inside its repeat envelope (`inside=4`, `outside=0`), while `repair-heavy-5s-guarded` has `inside=2`, `outside=2`, `outside-neg=1`, `outside-pos=1`, repeat range `0..+20`, candidate range `-35..+35`, and mean absolute beyond-envelope delta `17.5`. This confirms the typed-row movement is explainable as repeat noise in this focused run, while the expansion-comparison seed pair still shows real guarded-policy volatility outside the repeat envelope.
- The registered expansion component-probe artifact `cross-mode-budget-ablation-2026-05-06-product-corpus-short-budget-expansion-component-probes-5s-row-diagnostics` is generated under `artifacts/cross-mode-budget-ablations/2026-05-06/product-corpus-short-budget-expansion-component-probes-5s-row-diagnostics/`. It compares `baseline`, `baseline-repeat`, `lns-seed-short-5s-guarded`, `lns-repair-time-5s-guarded`, `cp-sat-reserve-5s-guarded`, and `repair-heavy-5s-guarded` on `expansion-comparison-replay`, budget `5s`, seeds `19` and `37`, mode `auto`, for 12 total Auto runs and 60 budgeted mode-seconds. The three single-component probes all remain inside the repeat envelope with mean Auto delta `0`, while the combined guarded policy is top with mean Auto delta `+17.5`, `inside=1`, `outside=1`, and a seed `19` LNS repair gain of `+35`. Treat this as evidence of a timing-interaction effect, not a standalone seed/repair/reserve knob and not promotion evidence.
- The registered expansion pairwise-interaction artifact `cross-mode-budget-ablation-2026-05-06-product-corpus-short-budget-expansion-pairwise-interaction-probes-5s-row-diagnostics` is generated under `artifacts/cross-mode-budget-ablations/2026-05-06/product-corpus-short-budget-expansion-pairwise-interaction-probes-5s-row-diagnostics/`. It compares `baseline`, `baseline-repeat`, `lns-seed-repair-5s-guarded`, `lns-seed-reserve-5s-guarded`, `lns-repair-reserve-5s-guarded`, and `repair-heavy-5s-guarded` on `expansion-comparison-replay`, budget `5s`, seeds `19` and `37`, mode `auto`, for 12 total Auto runs and 60 budgeted mode-seconds. Baseline ties the seed+repair, repair+reserve, and combined policies on mean Auto `762.5`, while `baseline-repeat` and seed+reserve fall to `745`. The repeat envelope itself is `-35..0`; seed+repair, repair+reserve, and combined each show the same `-35/+35` paired swing with `inside=1`, `outside=1`, while seed+reserve stays inside the negative repeat envelope and adds no positive repair. This points to repair-time participation as necessary for the upside, but the downside remains large enough to block promotion.
- The registered focused pairwise typed/expansion artifact `cross-mode-budget-ablation-2026-05-07-product-corpus-short-budget-focused-pairwise-typed-expansion-5s-row-diagnostics` is generated under `artifacts/cross-mode-budget-ablations/2026-05-07/product-corpus-short-budget-focused-pairwise-typed-expansion-5s-row-diagnostics/`. It compares `baseline`, `baseline-repeat`, `lns-seed-repair-5s-guarded`, `lns-seed-reserve-5s-guarded`, `lns-repair-reserve-5s-guarded`, and `repair-heavy-5s-guarded` on `typed-footprint-pressure` and `expansion-comparison-replay`, budget `5s`, seeds `19` and `37`, mode `auto`, for 24 total Auto runs and 120 budgeted mode-seconds. Combined guarded remains top on this tiny slice with mean Auto delta `+7.5`, but still has 1/4 paired regressions with worst delta `-5`. The seed+reserve pair is the cleaner narrow candidate: mean Auto delta `+5`, 0/4 regressions, candidate range `0..+20`, and only one positive outside-repeat-envelope row. Repair-time pairings remain unsafe: seed+repair has mean Auto delta `-1.25`, 2/4 regressions, and a `-35/+35` expansion swing, while repair+reserve has mean Auto delta `-5`, 2/4 regressions, a typed seed `37` delta of `-20`, and the same expansion swing. This revises the expansion-only interpretation: repair-time pairings can expose upside, but they also carry the blocked downside; seed+reserve is the next bounded product-corpus safety candidate, not promotion evidence.
- The registered product-corpus seed+reserve safety preflight `cross-mode-budget-ablation-2026-05-07-product-corpus-short-budget-seed-reserve-5s-auto-safety-preflight` is generated under `artifacts/cross-mode-budget-ablations/2026-05-07/product-corpus-short-budget-seed-reserve-5s-auto-safety-preflight/`. It compares `baseline`, `baseline-repeat`, and `lns-seed-reserve-5s-guarded` across all 10 product-corpus cases, budget `5s`, seeds `19` and `37`, mode `auto`, for 60 total Auto runs and 300 budgeted mode-seconds. Seed+reserve is top with mean Auto delta `+1.5`, 1/20 paired regressions, worst delta `-5`, best delta `+35`, CPU-budget efficiency ratio `1.004`, and mean wall-clock delta `-0.511s`. All candidate deltas are inside the same-slice repeat envelope (`inside=20`, `outside=0`, repeat range `-20..+35`, candidate range `-5..+35`), so the remaining typed-footprint seed `37` loss is variance-envelope-covered rather than a new outside-envelope policy failure. This is encouraging safety evidence for seed+reserve, but not promotion evidence because the paired regression is still present and the sweep is 5s-only over two seeds.
- The registered focused typed-footprint seed `37` repeat artifact `cross-mode-budget-ablation-2026-05-07-product-corpus-short-budget-seed-reserve-typed-footprint-seed37-repeat-5s-row-diagnostics` is generated under `artifacts/cross-mode-budget-ablations/2026-05-07/product-corpus-short-budget-seed-reserve-typed-footprint-seed37-repeat-5s-row-diagnostics/`. It reruns `baseline`, `baseline-repeat`, and `lns-seed-reserve-5s-guarded` on the remaining product-corpus seed+reserve regression row (`typed-footprint-pressure`, budget `5s`, seed `37`) for 3 total Auto runs and 15 budgeted mode-seconds. All three policies tie at Auto population `505`, with no paired regression and no outside-envelope movement. The product-corpus `-5` seed+reserve loss therefore did not reproduce on the focused repeat; it depended on baseline finding a late LNS repair to `525`, while this repeat left baseline, repeat, and seed+reserve at `505`. Treat the row as repeat-sensitive rather than a stable seed+reserve failure, but keep the policy diagnostics-only until a broader repeat or seed sweep confirms the product-corpus safety signal.
- The registered product-corpus seed+reserve seed sweep `cross-mode-budget-ablation-2026-05-07-product-corpus-short-budget-seed-reserve-5s-seed-sweep-auto-safety` is generated under `artifacts/cross-mode-budget-ablations/2026-05-07/product-corpus-short-budget-seed-reserve-5s-seed-sweep-auto-safety/`. It compares `baseline`, `baseline-repeat`, and `lns-seed-reserve-5s-guarded` across all 10 product-corpus cases, budget `5s`, seeds `7`, `19`, and `37`, mode `auto`, for 90 total Auto runs and 450 budgeted mode-seconds. The broader seed sweep blocks seed+reserve: `baseline-repeat` is top, while seed+reserve has mean Auto delta `-1.167`, 1/30 regression, worst delta `-100`, and one outside-negative repeat-envelope row. The stable failure is `multi-anchor-road-components` seed `7`, where shortening the LNS seed slice prevents the baseline `+100` LNS gain from reproducing. Do not promote seed+reserve broadly.
- The registered CP-SAT-reserve-only seed sweep `cross-mode-budget-ablation-2026-05-07-product-corpus-short-budget-cp-sat-reserve-5s-seed-sweep-auto-safety` is generated under `artifacts/cross-mode-budget-ablations/2026-05-07/product-corpus-short-budget-cp-sat-reserve-5s-seed-sweep-auto-safety/`. It compares `baseline`, `baseline-repeat`, and `cp-sat-reserve-5s-guarded` across all 10 product-corpus cases, budget `5s`, seeds `7`, `19`, and `37`, mode `auto`, for 90 total Auto runs and 450 budgeted mode-seconds. The probe improves `service-local-neighborhood` in two rows (`+30` and `+25`) but is blocked globally: mean Auto delta is `-1.833`, there are 2/30 regressions, both outside-negative repeat-envelope rows are `row0-corridor-repair-pressure` (`-55` at seeds `7` and `37`), and the CP-SAT stage fails to recover the baseline's late `+55` exact improvement. Do not promote CP-SAT reserve broadly.
- The registered conditional service-pressure reserve sweep `cross-mode-budget-ablation-2026-05-07-product-corpus-short-budget-service-pressure-cp-sat-reserve-5s-seed-sweep-auto-safety` is generated under `artifacts/cross-mode-budget-ablations/2026-05-07/product-corpus-short-budget-service-pressure-cp-sat-reserve-5s-seed-sweep-auto-safety/`. It compares `baseline`, `baseline-repeat`, and `service-pressure-cp-sat-reserve-5s-guarded` across all 10 product-corpus cases, budget `5s`, seeds `7`, `19`, and `37`, mode `auto`, for 90 total Auto runs and 450 budgeted mode-seconds. The conditional policy is top by mean Auto population (`+1.833`), has 0/30 regressions, and has no outside-negative repeat-envelope rows, but the score lift comes from repeat-sensitive `typed-footprint-pressure` and `expansion-comparison-replay` rows where the predicate did not apply. The targeted `service-local-neighborhood` rows all tie at `470`, so treat this as safe diagnostic plumbing and a possible future focused probe, not causal promotion evidence.
- The registered service-present seed+reserve sweep `cross-mode-budget-ablation-2026-05-07-product-corpus-short-budget-service-present-seed-reserve-5s-seed-sweep-auto-safety` is generated under `artifacts/cross-mode-budget-ablations/2026-05-07/product-corpus-short-budget-service-present-seed-reserve-5s-seed-sweep-auto-safety/`. It compares `baseline`, `baseline-repeat`, and `service-present-lns-seed-reserve-5s-guarded` across all 10 product-corpus cases, budget `5s`, seeds `7`, `19`, and `37`, mode `auto`, for 90 total Auto runs and 450 budgeted mode-seconds. Excluding service-free cases removes the prior `multi-anchor` failure, but the candidate is still blocked: `baseline-repeat` is top, service-present seed+reserve has mean Auto delta `+0.667`, 1/30 regression, worst delta `-15`, and one outside-negative repeat-envelope row on `typed-footprint-pressure` seed `37`. Shortening the LNS seed remains unsafe for footprint pressure even under a service-present predicate.
- The registered focused service-local reserve diagnostic `cross-mode-budget-ablation-2026-05-07-product-corpus-short-budget-service-local-reserve-5s-repeat-diagnostics` is generated under `artifacts/cross-mode-budget-ablations/2026-05-07/product-corpus-short-budget-service-local-reserve-5s-repeat-diagnostics/`. It compares `baseline`, `baseline-repeat`, and `service-pressure-cp-sat-reserve-5s-guarded` on `service-local-neighborhood`, budget `5s`, seeds `7`, `19`, `37`, `43`, and `61`, mode `auto`, for 15 total Auto runs and 75 budgeted mode-seconds. Baseline and the conditional reserve policy tie at Auto mean `470`, with 0/5 regressions and candidate range `0..0`; the policy applies on all five rows (`applied=5`, `inactive=0`). The same-slice `baseline-repeat` regresses on seeds `7` and `37` (`-30` and `-25`), so the earlier service-local upside is better interpreted as variance avoidance around the LNS repair path than as a reliable CP-SAT-reserve population lift.
- The registered focused typed-footprint seed-loss diagnostic `cross-mode-budget-ablation-2026-05-07-product-corpus-short-budget-typed-footprint-seed-loss-5s-repeat-diagnostics` is generated under `artifacts/cross-mode-budget-ablations/2026-05-07/product-corpus-short-budget-typed-footprint-seed-loss-5s-repeat-diagnostics/`. It compares `baseline`, `baseline-repeat`, `lns-seed-short-5s-guarded`, `cp-sat-reserve-5s-guarded`, and `lns-seed-reserve-5s-guarded` on `typed-footprint-pressure`, budget `5s`, seeds `7`, `19`, `37`, `43`, and `61`, mode `auto`, for 25 total Auto runs and 125 budgeted mode-seconds. Seed+reserve is top in this focused slice with mean Auto delta `+3`, but it still has 1/5 paired regressions with worst delta `-5`; its positive outside-repeat-envelope row is seed `43` at `+20`, and it has no outside-negative row. The shared loss is seed `61`: baseline reaches `525` by a `+20` LNS repair while baseline-repeat, CP-SAT reserve-only, seed-short, and seed+reserve all finish at `520` after a `+15` repair. Because the same `-5` appears with full `1s` LNS seed under `baseline-repeat` and CP-SAT reserve-only, the typed-footprint miss is repeat-sensitive LNS repair-depth variance rather than a seed-shortening causal failure. Treat the focused seed+reserve win as diagnostics-only, not promotion evidence.
- The registered focused expansion repair-volatility diagnostic `cross-mode-budget-ablation-2026-05-07-product-corpus-short-budget-expansion-repair-volatility-5s-repeat-diagnostics` is generated under `artifacts/cross-mode-budget-ablations/2026-05-07/product-corpus-short-budget-expansion-repair-volatility-5s-repeat-diagnostics/`. It compares `baseline`, `baseline-repeat`, `lns-repair-time-5s-guarded`, `lns-seed-repair-5s-guarded`, `lns-repair-reserve-5s-guarded`, and `repair-heavy-5s-guarded` on `expansion-comparison-replay`, budget `5s`, seeds `7`, `19`, `37`, `43`, and `61`, mode `auto`, for 30 total Auto runs and 150 budgeted mode-seconds. `lns-seed-repair-5s-guarded` is top by mean Auto delta (`+7`) but still has a paired regression with worst delta `-35`; every repair-time candidate keeps a candidate range of `-35..+35` and at least one regression. The same-slice repeat envelope is already wide (`-35..0`) because `baseline-repeat` misses seeds `37` and `43`, so the repair-time probes have no outside-negative rows, but they add outside-positive `+35` wins without removing the late-repair miss. Treat repair-time as exposing path-sensitive expansion upside, not as a bounded policy knob.
- Auto stage summaries now preserve compact per-neighborhood LNS outcomes (`phase`, iteration, status, score delta, CP-SAT status, and window coordinates) before later stage handoffs can overwrite the final incumbent metadata. Cross-mode telemetry emits those preserved outcomes as `lns-neighborhood` stages, so future `autoReplayDiagnostics` rows can report concrete windows instead of `not-captured` after a CP-SAT handoff.
- The registered targeted stage-diagnostics artifact `cross-mode-budget-ablation-2026-05-08` is generated under `artifacts/cross-mode-budget-ablations/2026-05-08/product-corpus-targeted-auto-lns-stage-diagnostics-5s/`. It compares `baseline`, `baseline-repeat`, `lns-repair-time-5s-guarded`, `lns-seed-repair-5s-guarded`, `lns-repair-reserve-5s-guarded`, and `repair-heavy-5s-guarded` on the two active investigation rows (`typed-footprint-pressure` and `expansion-comparison-replay`), budget `5s`, seeds `19` and `37`, mode `auto`, for 24 total Auto runs and 120 budgeted mode-seconds. This rerun did not reproduce any nonzero paired Auto deltas: all policies tied baseline mean Auto `652.5`, regression count `0/4`, and every candidate stayed inside the `baseline-repeat` envelope. The telemetry nevertheless confirms the new evidence path: all 24 Auto runs carry 3-4 preserved LNS neighborhood outcomes, and `expansion-comparison-replay` seed `37` baseline records the winning escalated full-grid window (`stageIndex=2`, iteration `2`, `6x6`, `+35` to `780`). Treat this as diagnostics plumbing plus repeatability evidence; it still does not justify a default Auto policy change.
- The registered full expansion stage-trace repeat artifact `cross-mode-budget-ablation-2026-05-08-product-corpus-expansion-repair-volatility-stage-trace-5s-repeat-diagnostics` is generated under `artifacts/cross-mode-budget-ablations/2026-05-08/product-corpus-expansion-repair-volatility-stage-trace-5s-repeat-diagnostics/`. It reruns the same expansion-comparison 5s repair-volatility slice as the 2026-05-07 artifact with preserved Auto LNS stage traces: `baseline`, `baseline-repeat`, `lns-repair-time-5s-guarded`, `lns-seed-repair-5s-guarded`, `lns-repair-reserve-5s-guarded`, and `repair-heavy-5s-guarded`, seeds `7`, `19`, `37`, `43`, and `61`, mode `auto`, for 30 total Auto runs and 150 budgeted mode-seconds. The prior `-35/+35` expansion swing did not reproduce: every policy and every seed reached Auto population `780`, with mean Auto delta `0`, 0/5 regressions for every candidate, and a zero-width baseline-repeat envelope. The artifact still validates the new trace path: 115 preserved LNS neighborhood outcomes across 30 Auto runs, 30 improved neighborhoods, and candidate seed `37` under `lns-seed-repair-5s-guarded` records the `+35` escalated repair at `stageIndex=2`, iteration `1`, window `5x6` from top-left `1,0`. Treat the old expansion failure as path-sensitive/repeat-sensitive and closed for now; do not spend more tuning time on repair-time pairings without a fresh reproducible outside-envelope failure.

Concrete work:

- Keep the current Auto budget policy as the baseline; the focused ablation did not justify a broad LNS-heavy or CP-SAT-reserve-heavy default.
- Do not promote or further tune `repair-heavy-5s-guarded` as a near-term default candidate unless a new policy first clears product-corpus paired Auto safety without short-budget regressions; the 5s triangulation still has a paired typed regression even when its tiny-slice mean is positive.
- If Auto policy work resumes, gate candidate deltas against a same-slice `baseline-repeat` envelope before interpreting small paired wins or losses, and inspect `policyApplicationSummary` before attributing full-corpus movement to a conditional policy. No current seed/repair/reserve candidate clears the broad product-corpus seed sweep: seed+reserve is blocked by `multi-anchor-road-components`, CP-SAT reserve is blocked by `row0-corridor-repair-pressure`, and service-present seed+reserve is blocked by `typed-footprint-pressure`. The service-local conditional reserve probe is safe on the focused row but ties baseline rather than improving it, the focused typed-footprint seed-loss replay diagnoses the current footprint miss as repeat-sensitive LNS repair-depth variance rather than seed-shortening causality, and the regenerated focused expansion replay shows the prior repair-time `-35/+35` swing is not reproducible in the latest same-slice stage-trace run. Do not pursue repair-time pairings as default candidates without a fresh reproducible outside-envelope failure.
- Treat outcomes as evidence for future policy work only; do not change Auto defaults without clearing the promotion gates below.

Exit criteria:

- A registered artifact explains whether the misses are seed quality, LNS repair allocation, CP-SAT reserve, or case-specific saturation effects. The 2026-05-06 focused and product-corpus preflight artifacts satisfy this for the known 1s/5s miss slice and leave default Auto unchanged.
- Any proposed Auto policy change has protected development and holdout evidence, worst-decile safety, and CPU-budget efficiency reporting.

## Gated Priorities

These are not next actions by default. They become active only when their trigger is satisfied.

| Trigger                                                                                        | Candidate Work                                         | Success Signal                                                                                                                                       |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| CP-SAT semantics and product corpus stay stable, and exact search quality remains a bottleneck | Geometry-native CP-SAT / `NoOverlap2D` experiment      | Better propagation or time-to-best without model-size blowup.                                                                                        |
| Service-master scorecards show repeatable equal-budget wins                                    | Promote service-master into Auto or Greedy seed policy | Beats current Auto or Greedy seed path on development and holdout pressure families with evaluator-valid layouts.                                    |
| Strict LNS label artifacts pass readiness and offline baselines                                | Learned LNS window ranking                             | Offline holdout beats deterministic, random, and single-feature baselines; online A/B improves fixed-budget quality without worst-decile regression. |
| Greedy offline ranker evidence is paired with online equal-budget wins                         | Feature-flagged learned Greedy re-ranking              | Online seeded benchmarks improve population or time-to-best with bounded inference overhead.                                                         |
| Portfolio scorecards show wall-clock and CPU-normalized wins                                   | CP-SAT portfolio in Auto                               | Portfolio beats single CP-SAT on quality and CPU efficiency.                                                                                         |
| A CPU-first label, training, feature, or inference workflow becomes a measured bottleneck      | GPU acceleration                                       | GPU reduces the measured bottleneck while preserving solver quality gates.                                                                           |
| Hosted or multi-user execution becomes a product requirement                                   | Durable worker architecture                            | Status, cancellation, and snapshots survive process restarts and multi-instance routing.                                                             |
| Exact bounds or incumbents remain blocked after CP-SAT tuning                                  | External MILP/SCIP/Gurobi/cuOpt research adapter       | Better bounds or incumbents on selected families under exact evaluator validation.                                                                   |

## Promotion Gates

Any default-path solver change must include:

- Exact validation for all final layouts.
- At least 3 fixed seeds.
- 1s, 5s, 30s, and 120s budget reporting when relevant.
- Protected development and holdout scorecards.
- Median population improvement, or equal population with at least 10% faster time-to-best.
- Worst-decile population delta `>= 0` unless a reviewed exception is documented.
- Regression rate `<= 5%`.
- CPU-budget efficiency no worse than 10% below baseline unless population improvement justifies it.
- Registered benchmark, hardware, split, command, model, artifact, and decision metadata.

## Guardrails

- Roads are support cells, not the primary objective.
- The formal road rule is per-component anchor connectivity; solver backends and validators must agree.
- Final road cleanup should remove only roads that do not affect building access or anchor-boundary road connectivity.
- Auto LNS stages must preserve reserved CP-SAT time unless a future trace-backed policy changes that.
- Learned guidance is not ready until traces show repeated ranking mistakes and enough counterfactual labels exist.
- Tiny saturated cases are smoke tests, not promotion evidence.
- Dynamic programming is a bounded exact subroutine for tiny windows and oracles, not a replacement for Greedy/LNS/CP-SAT.
- CPU parallelism must be measured against both wall-clock and CPU-second cost.
- Distributed solving should wait until hosting requires durable jobs or single-machine policy is no longer the bottleneck.
