This artifact is retained only as negative diagnostics evidence.

The `protected-neutral-weight0.0002` branch was superseded by the documented
`protected-neutral-weight0.0001` split-objective evidence. This threshold sweep
is unsafe at the top-mean threshold, so its registry draft was removed to prevent
accidental promotion or registry append.
